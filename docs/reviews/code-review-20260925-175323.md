# Code Review

## Scope

- Mode: current changes
- Branch or PR: fix/shared-provider-resume
- Base: HEAD (62fe9ca) — 仅未提交改动（9 modified + 1 untracked）；已提交的 5 个 branch commit 只作上下文，不在本次 findings 范围内
- Output file: docs/reviews/code-review-20260925-175323.md
- Included scope:
  - scripts/patch-codex-model-catalog.py（第三方 catalog 生成器重写：会话模型条目注入 + --cards-dir/--output-dir）
  - scripts/sync-codex-agent.sh（事务化部署：staging → 生成 → 安装）
  - codex-agent/profiles/{ds-flash,glm,glm-flash,local}/config.toml（新增 model_catalog_json，其余五张卡已有）
  - scripts/agent-tools.zsh（gpt_codex alias，line 545）
  - README.md / README.zh.md（九 profile 目录补丁说明、事务化 sync 说明、gpt_codex 说明）
  - tests/test_model_catalog_generation.py（新增）、tests/test_repair_codex_reasoning_history.py（alias 用例）
  - 部署链路实证：sync-codex-agent.sh → patch-codex-model-catalog.py → codex -p <profile> 加载
- Excluded scope: auth.json、会话正文、API key（未读取）；网关真实往返（需密钥与网络）；business 独立 home
- Parallel review coverage: 无（任务要求不派发子 Agent）
- Reviewed-state（review 期间工作区曾被并发编辑，最终审查状态以 SHA-256 钉死）:
  - patch-codex-model-catalog.py aa47d3ea…b662d2 / sync-codex-agent.sh b94a98e5…b96bc / agent-tools.zsh 4e8b6d63…0caac7
  - test_model_catalog_generation.py eeaaee4a…460c2 / test_repair_codex_reasoning_history.py 76828cb3…f400
  - README.md 33b3e7a6…e663 / README.zh.md 3c120a84…e204

实证记录（本机、只读或临时目录内执行）：

- 本机 Codex bundled catalog（fresh CODEX_HOME 下 `codex debug models`）：11 个模型；`codex-auto-review.tool_mode == "code_mode_only"`（与 EXPECTED_BEFORE 一致）；`gpt-5.4` 存在、有 base_instructions、`tool_mode` 键缺省（null）、max_context_window=1000000。
- 用真实 codex + 桩 launchctl 把当前 sync-codex-agent.sh 完整跑入临时 CODEX_SHARED_HOME：exit 0；安装 12 张卡 + 9 份 catalog；无 @HOME@ 残留；每份 catalog 12 个模型、会话条目 window 与卡一致、tool_mode=direct、guardian=direct。
- 九张卡逐张 `codex -p <profile> debug prompt-input "hi"`：rc=0、stderr 无告警；负对照（catalog 文件缺失→os error 2、catalog JSON 损坏→parse error，均 rc=1）证明 catalog 文件确实被读取解析。
- `python3 -m unittest discover -s tests`：23 tests OK。

## Findings

### 1-未修复-中-会话条目模板未校验 direct-tool 形状，schema 漂移时静默放行
- **入口/函数**: `session_entry()`，由 `write_targets()` 对每个 profile 调用
- **文件(行号)**: scripts/patch-codex-model-catalog.py:146-184（校验仅 151-154；强制覆写 156-183）
- **输入场景**: Codex 升级后 bundled catalog 中 `gpt-5.4` 的 tool_mode 变为 `code_mode_only`，或其 direct 形态相关字段（如 `experimental_supported_tools`、`shell_type`、`use_responses_lite` 等未清洗继承字段）改形
- **实际分支**: 151-154 只检查模板存在且 `base_instructions` 是 str；随后 162 行强制 `tool_mode="direct"` 覆写 deepcopy 的模板，生成成功、exit 0
- **预期行为**: 按本脚本自定契约（docstring 16-18 "uses a current bundled direct-tool model as its schema template"；25-27 "compares against the expected shape and warns loudly when the format drifts"），模板不再满足 direct-tool 形状时应像 guardian 分支（112-119，EXPECTED_BEFORE 不符即拒写）一样 fail-closed
- **实际行为**: 模板形状无校验。对照证据：guardian 条目有严格前置值校验（fail-closed），模板只有存在性校验（fail-open）。本机实测当前 gpt-5.4 的 `tool_mode` 键本来就缺省（null）——"direct-tool 模板"这一前提目前是隐含事实而非被校验事实。且 verify()（228 行）复查的是脚本自己写入的值，无法发现此类不一致
- **直接证据**: scripts/patch-codex-model-catalog.py:151-154（仅 `base_instructions` 检查）vs 112-119（guardian 严格校验）；162 行无条件 `tool_mode="direct"`；实测 bundled catalog gpt-5.4 tool_mode=null
- **影响**: Codex 升级触发时，九份 schema 不一致的 catalog 经事务化 sync 正常部署（所有校验全绿），九个 profile 的 tool calling 在运行时才暴露；可恢复（修生成器后重跑 sync）但用户可见且诊断链路长
- **建议改法和验证点**: 在 151-154 增加模板不变量断言，例如 `template.get("tool_mode") in (None, "direct")`，并对若干 direct 形态关键字段做存在性/取值钉住；不满足则 warn + 跳过全部 profile（exit 1）。验证点：构造 tool_mode="code_mode_only" 的假 source catalog，断言生成器非零退出且 staging 目录为空
- **修复风险（低/中/高）**: 低（纯新增拒写分支，不改成功路径）
- **严重程度（低/中/高/严重）**: 中

### 2-未修复-低-provider registry 在事务边界外先提交，catalog 失败后 base config 与已装卡可能跨代混配
- **入口/函数**: sync-codex-agent.sh 主流程
- **文件(行号)**: scripts/sync-codex-agent.sh:32-34（registry 直写 live base config.toml）先于 37-74（staging → 生成 → 原子安装的事务边界）
- **输入场景**: 未来某次 model-providers.toml 删除/重命名某 provider，同次 sync 中 catalog 生成失败（如 Codex 改了 catalog 形状）
- **实际分支**: registry 已写入新 block（旧卡引用的 provider 可能已消失）→ 生成器非零退出 → `set -e` 中止 → 已装卡与 catalog 保持旧版不变
- **预期行为**: README.md:287-289 承诺 "sync stages all cards and catalogs first, so installed cards and catalogs stay intact"——对已装卡而言，其运行依赖（provider 注册表）也应保持一致
- **实际行为**: 卡与 catalog 确实原样，但 base config 的 provider block 已推进一代；旧卡若引用被移除的 provider，`codex -p <旧卡>` 将解析失败——"保持原样"的承诺在跨文件依赖上不成立
- **直接证据**: scripts/sync-codex-agent.sh:32-34 在 37 行 staging 之前且无回滚；sync-codex-providers.py 为直写式原子替换（其 docstring "Atomically sync repository-owned provider tables into a Codex base config"）
- **影响**: 仅局部行为错误，且需要"registry 缩减 + 同次 catalog 失败"两个条件叠加；当前 diff 未改 model-providers.toml，现实触发概率低
- **建议改法和验证点**: 短期在 sync 注释/README 写明事务边界不含 provider block（additive-only 是隐含前提）；长期可把 registry compose 也纳入 staging（先 compose 到临时文件，事务提交点一并安装）。验证点：桩 codex 输出坏 catalog + 删减版 registry，断言 base config 未被推进
- **修复风险（低/中/高）**: 低（文档化）/ 中（staging 化改动安装顺序）
- **严重程度（低/中/高/严重）**: 低

### 3-未修复-低-独立运行生成器时失败只保留"未轮到"的 profile，已写 profile 直接落在 live 目录
- **入口/函数**: `write_targets()` 独立运行路径（默认 --output-dir）
- **文件(行号)**: scripts/patch-codex-model-catalog.py:187-210（逐 profile 直接写）、238（--output-dir 默认 CATALOG_DIR，即 live 目录）
- **输入场景**: 按 docstring Usage（29-35 行）手工独立运行 `patch-codex-model-catalog.py`，九个 profile 中第 k 个卡损坏（如本地手改了 ~/.codex/glm.config.toml）
- **实际分支**: 前 k-1 个 profile 的新格式 catalog 已写入 live `~/.codex/model-catalogs/`，第 k 个 warn 跳过，exit 1
- **预期行为**: "失败时既有 catalog 保留"这一性质在文档化的独立用法下同样成立，或 Usage 明确声明只有经 sync 调用才有整体保留保证
- **实际行为**: 部分 profile 的 live catalog 被推进、部分保留旧版；每份 catalog 自包含且各 profile 独立，旧 profile 不受影响，但同一 profile 处于新格式的代价是：若新格式本身有问题（见 finding 1），独立运行没有 staging 兜底
- **直接证据**: scripts/patch-codex-model-catalog.py:199-209（循环内逐个 os.replace 到 output_dir）；sync-codex-agent.sh:55-57（sync 路径才传 --output-dir 到 staging）
- **影响**: 仅局部行为错误；sync 路径（真实部署链路）已由 staging + test_sync_keeps_installed_cards_when_catalog_generation_fails 覆盖，影响限于手工调试场景
- **建议改法和验证点**: 在 docstring Usage 段补一句"standalone 运行逐 profile 直接写 live 目录；整体保留保证仅经 sync-codex-agent.sh 提供"，或令独立模式也默认先写临时目录再统一安装。验证点：构造一好一坏两张卡，独立运行后断言好卡 profile 的 catalog 状态与文档承诺一致
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

- 会话条目钉死 `supported_reasoning_levels=[]` + `default_reasoning_level=null`（patch-codex-model-catalog.py:164-165），而九张卡均设 `model_reasoning_effort="max"`（local 为 "xhigh"）。实证：九 profile `prompt-input` 加载均 rc=0，config 期不拒绝；但 Codex 是否仍把卡的 effort 透传给网关、网关是否接受，静态不可证。改动前 fallback 元数据的 levels 取值同样未知，无法判定是否为行为回退。
- `input_modalities=["text","image"]`（182 行）对九个 profile 一律声明图像输入；deepseek 网关与本地 llama.cpp 构建的图像能力未验证，旧 fallback 的假设也未知。若网关不接受图像，用户 `codex -i` 附图时客户端放行、网关侧报错。
- catalog 与 config 的 compaction 优先级：条目继承模板的 `effective_context_window_percent=95`，各卡显式设 `model_auto_compact_token_limit`（1048576 窗的 75%、local 的 87.5%）。docstring 已证明 catalog max 会压过 config window（"clamps even an explicit model_context_window override"），同类优先级若作用于 compact limit，压缩点将晚于卡的意图；仓库内无可判定证据。
- 九个第三方模型的系统提示继承 gpt-5.4 的 `base_instructions`（模板 deepcopy 未覆写该字段）；这是 docstring 声明的刻意设计，但其随 Codex 升级的漂移面未被任何校验覆盖（与 finding 1 同源但语义不同：即使模板保持 direct，提示词内容变化也无告警）。

## Residual Risk

- 仓库测试覆盖：生成正确性（真实 catalog + 卡窗口）与失败保留（桩 codex 输出 `{}` → sync 中止、卡与 catalog 原样）已有自动化；但"成功路径的完整 sync"（staging 安装 12 卡 + 9 catalog 到目标 home）无自动化测试——本次 review 在临时 home 手工验证通过（exit 0、产物逐项正确），未固化进测试套件。
- 未做真实网关往返：九 profile 的实际派发、guardian（codex-auto-review）升级审批在 patched catalog 下的行为、resume 跨 provider 链路，均需密钥/网络，本次仅验证到"catalog 被真实加载解析、profile layering 生效"（负对照证明加载路径真实）。
- catalog 测试类带 `@skipUnless(shutil.which("codex"))`（test_model_catalog_generation.py:20）：无 codex 的机器上整个 catalog 面静默跳过，CI 若缺 codex 将失去该回归面。
- review 期间工作区被并发编辑（test_model_catalog_generation.py 于 17:45:30 新增第二用例、sync 脚本 17:44 事务化重写、README 17:46 改动）；最终审查对象以 Scope 中 SHA-256 为准，之后的任何修改不在本次结论内。
- live 机器尚未同步：`~/.codex/model-catalogs/` 当前仅有旧格式五份（kimi/mimo×3/qwen，无会话条目），ds-flash/glm/glm-flash/local 的 live 卡尚无 `model_catalog_json`；行为变化在下次 sync 才生效，本次未对 live 配置做任何写入（只读检查）。
- `verify()` 是自指校验（重读脚本自写文件、复查脚本自设字段），能发现写入损坏，不能发现"合法 JSON 但 schema 语义漂移"——该缺口即 finding 1。

## Controller Adjudication (after reviewer completion)

- **Finding 1 — accepted and fixed.** `session_entry()` now rejects a template whose tool mode is neither absent nor `direct`, whose shell type is not `unified_exec`, or whose Responses Lite / experimental tool fields differ from the verified shape. A regression test changes the source template to `code_mode_only` and confirms a nonzero exit without replacing the prior catalog.
- **Finding 2 — rejected for this change.** The provider registry is managed by `sync-codex-providers.py` and is unchanged in this diff. The project contract in both READMEs explicitly retains retired provider IDs, so the suggested provider-deletion scenario violates the existing registry invariant. Catalog/card staging does not claim a transaction over the provider registry.
- **Finding 3 — accepted as a documentation clarification.** The generator docstring now states that standalone mode writes one profile at a time, while `sync-codex-agent.sh` stages all generated catalogs before installation.
- **Open questions resolved from Codex 0.157.0 source.** Unknown-model fallback already uses empty `supported_reasoning_levels` and default text/image modalities (`models-manager/src/model_info.rs`). `ModelInfo::auto_compact_token_limit()` takes the minimum of the configured threshold and 90% of the resolved window (`protocol/src/openai_models.rs`), so the cards' explicit thresholds remain effective. Borrowing the bundled template's base instructions is an intentional remaining prompt-drift risk.
- **Post-review validation.** The two `test_model_catalog_generation.py` tests now have separate skip conditions, so the failure-preservation test runs without Codex installed. The full suite passes (23 tests); `git diff --check`, Bash and Zsh syntax checks pass. An isolated Kimi request with the generated catalog completed a `pwd` tool call and recorded a 996,147 effective window. Live sync then installed nine catalog files and all cards; a new shell resolves `gpt_codex` to `gpt-6-sol`. The reviewer's prior live-state observation was accurate at review time and is now superseded.
