Reviewer: Claude/MiMo

# Code Review

## Scope

- Mode: current changes（`$deepreview --base main` 方法；任务标签 `mimo-resume-review-20260925`）
- Branch: `fix/shared-provider-resume`（1 个领先提交 `7f43e75 fix(codex): register shared providers for session resume` + 未提交 README 修订）
- Base: `main` @ `4aa780f`
- Output file: `docs/reviews/code-review-20260925-100425.md`（由总控保存；reviewer 按只读约束未写入仓库）
- Included scope：#39 全部改动 —— `codex-agent/model-providers.toml`（新增 9 provider 共享注册表）、9 张第三方卡（移除卡内 `[model_providers.*]`，`glm-flash`/`mimo-fast`/`mimo-flash` 改独立 ID）、`scripts/sync-codex-providers.py`（新增标记块原子同步器）、`scripts/sync-codex-agent.sh`、`scripts/compose-codex-app-config.py`、`tests/test_provider_registry.py`（新增 9 用例）、`README.md`/`README.zh.md`（含未提交修订）、`docs/reviews/` 5 份已提交审查/裁决 artifact；契约面走读（未改动）：`scripts/agent-tools.zsh`（`-p`/resume 接线、key/port 映射）、`codex-agent/shim-routes.json`、`codex-agent/bin/codex-auto-review-shim`、`scripts/codex-agent-run`、`scripts/patch-codex-model-catalog.py`；live 部署面核对：`~/.codex/config.toml` 结构（仅 grep 段头/标记/根键名）
- Excluded scope（按任务约束）：`auth.json`、会话正文、任何发往模型的请求；`~/.codex-agent/business`（独立 home，本就不管）；Codex 二进制内部 resume/config 合并语义（闭源，见 Residual Risk）
- Parallel review coverage: 无（任务禁止派发子 Agent；全部走读由本 reviewer 完成）
- 限制说明：`gh pr view 39` 因 TLS 证书校验失败无法获取 PR 元数据，PR title/description 未纳入走读；以分支提交与完整 diff 替代
- 项目指令检查：仓库内及 `~/.codex` 均**不存在 `AGENTS.md`/`CLAUDE.md`**（`find` 全仓无命中），无仓库级 agent 指令可对照；本报告以 README 双语版本作为契约文档

## Findings

### 1-未修复-低-README 目录树漏掉新增的 `tests/`，与本次修订自己写入的测试入口不一致
- **入口/函数**: README 文档修订（`README.md` / `README.zh.md` 的 Repository Layout 树 + Validation 命令）
- **文件(行号)**: `README.md:486-525`（树块，`codex-agent/`、`scripts/` 逐文件列出但无 `tests/`）与 `README.md:548-552`（`python3 -m unittest discover -s tests`）；ZH 同构（`README.zh.md:460-495` 与 `:517-521`）
- **输入场景**: 读者按 README 树了解受管源码布局，或按本次修订新增的 `python3 -m unittest discover -s tests` 入口找测试文件位置
- **实际分支**: 树块对 `scripts/` 列出了全部 11 个文件（含本次新增的 `compose-codex-app-config.py`、`sync-codex-providers.py`），对 `codex-agent/` 列出新增的 `model-providers.toml`，唯独 `tests/test_provider_registry.py`（本 diff 新增的受管源码）在树中无任何条目
- **预期行为**: 本次修订把测试套件引入仓库并在双语文档写明运行命令，树作为"source of truth"清单应同步收录 `tests/`
- **实际行为**: 树中缺 `tests/` 条目；`grep -n 'tests/' README.md` 除命令行外无命中
- **直接证据**: `git diff main...HEAD --stat` 显示 `tests/test_provider_registry.py | 168 +++++` 为受管新增文件；树块逐文件列表与 `ls scripts/` 对比仅缺 tests 目录
- **影响**: 仅文档完整性；读者按树定位测试文件会误以为测试不在仓库管理范围内（`README.md:19` 明说 repo 是 skills/launcher/runners/registry 的 source of truth，测试未被点名）
- **建议改法和验证点**: 树中补 `tests/test_provider_registry.py`（EN/ZH 同步）；验证点：树文件清单与 `git ls-files` 的人工维护源码集合一致
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

（代码改动本体——注册表、同步器、compose、卡片、测试——经逐路径走读与实测**未发现实质性 correctness/stability 问题**；此前两轮 review 的 10 项 finding 与 4 项复审 finding 均已按 `docs/reviews/provider-registry-adjudication-20260925-090857.md` 裁决，Accepted 项在当前代码中逐一验证为已修复，Deferred 项见 Residual Risk，无误裁。）

## Open Questions

- **OQ1（文档修订准确性，未验证断言）**：未提交修订声称"单独执行 `codex resume <session-id>` 可能恢复会话上次选中的模型，不会自动改成 base config 的默认模型"（EN: "Plain `codex resume <session-id>` can restore the session's last selected model instead of the base config's default"）。可验证的部分：base 默认 `model = "gpt-6-sol"` 确实存在（`~/.codex/config.toml:1`）；resume 会读取会话记录的 provider（裁决文档实测 `Model provider kimi not found`）；带 `-p` 时卡片胜出（实测进入 GPT-6-Sol TUI）。**"恢复会话上次模型"这半句在仓库代码与 `codex-cli 0.156.1` 的 `codex resume --help` 中均无直接证据**（help 只说明 `[SESSION_ID]`/`[PROMPT]` 与 `-m`/`-p` 覆盖，未说明默认模型来源）。另 EN 的 "can restore … instead of …" 有两可读法（"会恢复 X 而非 Y" vs "可能恢复 X、而非自动取 Y"），ZH 后半句 "不会自动改成" 是无限定断言，双语确定性不对称。建议：用一次 plain resume 观察 TUI 显示的模型后再落定措辞（本任务禁止触碰真实会话，无法代验），或两句都保留 "可能"。
- **OQ2**：PR #39 的 title/description/CI 状态未能读取（`gh` TLS 失败：`x509: OSStatus -26276`），若 PR 描述另有声称的行为未在 README/diff 中体现，本报告未覆盖。

## Residual Risk

- **R1（本轮最重要，验证缺口）**：跨 provider resume 后**续写 turn 的实际路由**从未实测（裁决文档明确记录 "Actual response routing after a continued prompt was not exercised"）。本 diff 的修复面是"历史 provider 可解析"（bootstrap 面，已实测），而未提交 README 现在**主动推荐**跨模型续接流程（`gpt-6-sol_codex resume <sid>`）。若 Codex 续写时粘住会话记录的旧 provider 而只显示新模型，续写请求会带着新模型名打到旧网关。验证成本低：kimi 会话 `-p gpt-6-sol` 续发 1 turn，看 shim 日志端口（应无 8790 流量）。
- **R2（legacy 变体 ID，裁决已 Deferred，shim 证据补充细化）**：`--check` 实测 shim 只改写 guardian slug（`model=kimi-k3` → `how=none` 直通；`codex-auto-review` → 路由 `to`）。因此旧 `glm`/`mimo` 会话的普通 turn 仍按会话记录的模型名直通，且 `glm`/`glm-flash`、`mimo`/`mimo-flash` 共享 upstream 与 `env_key`，**主要偏差只剩 guardian 升级审阅的改写模型**（8789→`glm-5.3`、8791→`mimo-v2.6-pro`）；真正受损的是 **legacy `mimo-fast` 会话**（记录 ID `mimo`）——恢复后走 8791/`token-plan-cn`，而 `mimo-v2.6-pro-ultraspeed` 只在 8794/`api.xiaomimimo.com`，普通 turn 会打错 upstream 被网关拒绝。README "cannot identify which variant…retain the normal routes" 的表述与此一致，作为已接受限制保留。
- **R3（裁决 Deferred，原样保留）**：注释标记被外部 config 重写丢失 → 下次 sync 误判 ownership conflict 整体中止（fail-closed）；并发写在最终内容检查与 `os.replace` 间仍有 TOCTOU 窗口（无共享锁）；从注册表移除的 provider 在已合成 app home 永久残留（本 change 无移除场景）。
- **R4（潜伏解析陷阱，当前无触发器）**：`compose-codex-app-config.py` 的 `split_sections`（`SECTION_RE = ^\[([^\]]+)\]\s*$`）不识别 `[[array]]` 头与带行尾注释的段头，会被吞进上一段 body；若上一段恰是被共享注册表覆盖的 `model_providers.*`，其内容会在合成 app home 时静默丢失。实测 live base config 零 `[[` 头、段头全部干净，**当前无触发面**；属既有 regex，本 diff 仅扩大了覆盖段集合。另：注册表可写入 `openai` 等内建 ID 遮蔽 gpt-6 三卡的隐式 provider，无测试禁止；registry→card 反向覆盖（死条目）无测试。
- **R5（闭源行为未钉版本）**：resume 的 model/provider 合并细节（thread 记录 vs 卡片覆盖的优先级、plain resume 默认模型）依赖 `codex-cli` 内部实现；本次仅验证 0.156.1 的 CLI 面（`codex resume -p <id> <sid>` 语法成立；`-p` 语义 "Layer $CODEX_HOME/<name>.config.toml on top of the base user config"；`codex exec resume` 链上 launcher 的 `exec -p <id> resume …` 放置解析通过）。升级 codex-cli 后建议复测 R1。
- **R6**：`test_registry_matches_shim_routes_and_launcher_keys` 的 zsh 半段在无 zsh 环境静默跳过（`shutil.which("zsh")` 守卫）；CI 未配置（无 workflow），回归只能靠本地 `python3 -m unittest discover -s tests`。

## Validation Performed

- **实测**：`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` → **9/9 OK**（本轮独立复跑，0.357s；与裁决文档一致）；`python3 codex-agent/bin/codex-auto-review-shim --check` 双探针（`kimi-k3` 直通 / `codex-auto-review` 改写）；`codex exec -p gpt-6-sol resume --help` 解析通过（launcher 的 `-p` 放置合法）；live `~/.codex/config.toml` 仅 grep 段头/标记：managed 标记块 244–318 行、9 个 `[model_providers.*]`、无 `[[` 头、无内联 `model_providers` 根键、`model = "gpt-6-sol"` 在位 —— 与注册表逐 ID 对齐，本机迁移窗口已关闭（12 张已部署卡为新格式）。
- **重点检查项结论**：
  1. **Codex resume 的 model/provider 选择**：修复主链成立——`codex resume`/`codex exec resume` 均合并共享 base（现含标记注册块）+ `-p` 卡片，历史 provider 任何卡下可解析（修复前实测报 `Model provider kimi not found`，修复后进入目标卡 TUI）；registry→cards→shim-routes→launcher 的 port/key 契约由测试锁定。缺口即 R1（续写 turn 路由）与 OQ1（plain resume 默认模型）。
  2. **Profile 覆盖**：无缺口。12 张卡 ↔ 9 个注册表 ID（ds-flash→deepseek:8788、glm→glm:8789、glm-flash→glm_flash:8795、kimi:8790、local→local_llama:8080 直连无 shim、mimo:8791、mimo-fast→mimo_fast:8794、mimo-flash→mimo_flash:8793、qwen:8792）+ gpt-6-astra/sol/luna 走内建 `openai`（卡不设 `model_provider`）；`hy` 仅 Claude runtime（Codex 侧卡已删，runner/launcher 均无 hy）、`business` 独立 home 不受管——与文档一致。
  3. **文档修订准确性**：已提交与未提交修订的结构性陈述（"base 仅标记块由仓库管理"、"冲突 ID 报错不覆盖"、"distinct provider IDs because distinct gateway ports"、"legacy ID 保留普通路由"、"app home 会话隔离"、launcher/`-p` 续接示例）逐条对照代码与 CLI 帮助**属实**；唯一未验证的行为断言与双语不对称见 OQ1，唯一实质文档缺口见 Finding 1。
- **约束遵守**：全程只读（测试仅写 `$TMPDIR`）；未修改仓库文件、未 commit/push/PR/merge、未派发子 Agent、未读取 `auth.json` 与会话正文、未向该用户会话发送模型请求。
