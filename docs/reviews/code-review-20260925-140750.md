RUNTIME/PROVIDER/MODEL: claude/kimi/kimi-k3[1m]

# Code Review

## Scope

- Mode: current changes
- Branch or PR: `fix/shared-provider-resume`（含 3 个未合并 commit：`7f43e75`、`edfd821`、`8324966`，以及 6 个文件的未提交改动）
- Base: `main`
- Output file: 建议 `docs/reviews/code-review-20260925-135059.md`（按任务约束未写仓库，artifact 由总控保存）
- Included scope: 相对 `main` 的全部改动，重点沿 `xx_codex --resume <session-id>` 真实入口逐行走读：`scripts/agent-tools.zsh`（`_codex_agent_launch` 参数解析、`--resume` 分支、`-p` 注入、business 特例）→ `scripts/repair-codex-reasoning-history.py`（`--session-id` 会话定位、UUID 校验、`--target-config` 解析、`keep_reasoning_model` 语义、备份/原子替换、错误路径）→ `codex resume` 调用构造；`scripts/sync-agent-tools.sh` 安装；`tests/test_repair_codex_reasoning_history.py`；`README.md`/`README.zh.md`；以及已提交的 provider registry 链路（`sync-codex-providers.py`、`model-providers.toml`、profile 卡、`compose-codex-app-config.py`）与 resume 的交互。
- Excluded scope: 真实 `~/.codex` 会话内容（仅只读确认了 business `config.toml` 存在顶层 `model` 键）；live 安装文件未修改（仅只读 diff）；早前已裁决的 rollout-path 模式修复（见 `docs/reviews/reasoning-history-adjudication-20260925.md`）未重复深挖。
- Parallel review coverage: 无（任务约束禁止派发子 Agent；全部由本 reviewer 走读 + 隔离探针验证）。

## Findings

### 1-未修复-低-`--resume=<id>` 等号形式不被拦截，直通 codex 报全局参数错误
- **入口/函数**: `_codex_agent_launch`（`gpt-6-sol_codex` 等全部 launcher）
- **文件（行号）**: `scripts/agent-tools.zsh:462`（`[[ "${codex_args[1]}" == --resume ]]` 精确匹配）；旁路出口在 `:493-494`
- **输入场景**: `gpt-6-sol_codex --resume=<session-id>`（用户按 CLI 习惯写 `--flag=value`）
- **实际分支**: `:462` 条件不命中 → 不修复、不校验 → `:493-494` 走 `*)` 分支注入 → 最终执行 `codex -p gpt-6-sol --resume=<id>`
- **预期行为**: 与空格形式一致地拦截并修复，或按 launcher 用法报错（rc 2）
- **实际行为**: codex 0.156.1 无全局 `--resume` 选项（`codex --help` 仅有 `resume` 子命令，已核实），codex 以参数解析错误退出；修复未运行
- **直接证据**: 隔离探针 7 的 stub 捕获输出为 `-p` / `gpt-6-sol` / `--resume=<session-id>`；`codex --help`/`codex resume --help` 输出中无全局 `--resume`
- **影响**: 仅局部行为错误（用户困惑）；fail-closed，会话文件未被触碰
- **建议改法和验证点**: 二选一 —— 在 `:462` 前对 `--resume=*` 形式取值拦截（`--resume=${id}` 拆出 id 复用现有分支），或在用法报错文案中明示仅支持空格形式；验证点：等号形式触发修复或明确用法错误，stub 不收到 `--resume=*`
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 2-未修复-低-repair 工具对 Python ≥ 3.11（`tomllib`）的硬依赖未声明、无版本守卫
- **入口/函数**: `repair-codex-reasoning-history.py` 模块导入期；经 `agent-tools.zsh:473` 直接以 shebang 执行
- **文件（行号）**: `scripts/repair-codex-reasoning-history.py:18`（`import tomllib`）；安装点 `scripts/sync-agent-tools.sh:15`；文档前置要求 `README.md:129-136` / `README.zh.md:116-123`
- **输入场景**: `python3` 指向 < 3.11 的机器（如旧 Xcode CLT 的 3.9）上执行任意 `xx_codex --resume <id>`
- **实际分支**: 模块导入即 `ModuleNotFoundError: No module named 'tomllib'` → traceback，exit 1 → launcher `:477` `return $?`
- **预期行为**: 干净的中文/英文错误提示并 fail-closed；或文档声明 Python ≥ 3.11 前置要求
- **实际行为**: Python traceback（未经 `:182-185` 的 OSError 清理路径，因为失败发生在 import 期）；README 前置要求只列 `zsh/claude/codex/jq/curl`，未提 Python 版本
- **直接证据**: `:18` 顶层 import；`tomllib` 仅 `:128` 的 `--session-id` 分支使用；README 前置要求清单无 Python 条目；本机 `python3 --version` = 3.11.15（live 不受影响）
- **影响**: 静默失效谈不上，但错误不可读；旧的 rollout-path 用法（不需要 tomllib）也被连带禁用。已提交的 `sync-codex-providers.py` 同样有 tomllib 依赖，但那是维护者手动 sync；本改动把它变成每次 `--resume` 的运行时依赖
- **建议改法和验证点**: 把 `import tomllib` 移入 `--session-id` 分支内部（lazy import）或在 main 开头做版本检查并给出干净报错；README 双语前置要求补 `python3 ≥ 3.11`。验证点：3.9 环境下 rollout-path 模式仍可用 / `--session-id` 模式报错无 traceback
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 3-未修复-低-launcher `--resume` 拒绝 codex resume 原生的可选 `[PROMPT]` 位置参数
- **入口/函数**: `_codex_agent_launch` 的 `--resume` 分支
- **文件（行号）**: `scripts/agent-tools.zsh:463-466`（要求恰好 2 个参数）
- **输入场景**: `gpt-6-sol_codex --resume <id> "继续上次的方向"` —— codex 0.156.1 `resume` 语法为 `codex resume [OPTIONS] [SESSION_ID] [PROMPT]`
- **实际分支**: `${#codex_args[@]} == 2` 不成立 → 用法错误，rc 2
- **预期行为**: 修复后把 prompt 透传给 `codex resume`，或在双语 README 明示该限制与两步替代路径
- **实际行为**: 拒绝执行；用户须先 `--resume <id>`（修复+进入 TUI），或手工跑 repair 脚本后用普通 `resume <id> "prompt"`。README 新增段落未说明这一点
- **直接证据**: `:463-466` 计数检查；`codex resume --help` 的 `[SESSION_ID] [PROMPT]` 位置参数说明；普通 `resume` 路径（`:490-491`）透传任意尾部参数不受限
- **影响**: 仅局部功能受限；有明确变通，无状态风险
- **建议改法和验证点**: 允许 `≥2` 个参数并把 `${(@)codex_args[3,-1]}` 附加到修复后的 `codex_args`；或 README 双语补一句限制说明。验证点：带 prompt 的 `--resume` 调用最终 argv 为 `resume -p <id> <id> <prompt>`
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 4-未修复-低-双语 README 对备份语义的表述是无条件的，与“无改动不备份”实现不符
- **入口/函数**: 文档（非代码路径）
- **文件（行号）**: `README.md:223-228`（"saves a private backup, removes reasoning records…"）；`README.zh.md:208-212`（"保存私有备份、删除…reasoning 记录"）；实现 `scripts/repair-codex-reasoning-history.py:152`（`if not args.apply or count == 0: return 0`）
- **输入场景**: 对本来就干净的会话运行 `gpt-6-sol_codex --resume <id>`（`affected reasoning items: 0`）
- **实际分支**: `:152` 直接 return 0 → 无备份、无写入 → 正常 resume
- **预期行为**: 文档说明“有待删记录时才保存备份；会话无需修复时不产生备份”
- **实际行为**: 文档语气无条件，用户事后按文档找备份文件会发现不存在，可能误判为失败
- **直接证据**: 隔离探针 6：第二次 `--resume` 输出 `affected reasoning items: 0`，备份数保持 1（实现行为正确且符合幂等设计）；README 两段新增文字均无此限定
- **影响**: 仅文档精度问题；实现行为本身正确
- **建议改法和验证点**: 双语各补一句，如 "a session with nothing to remove is left untouched and gets no backup" / "会话若无待删记录则不做改动也不生成备份"。验证点：文档与 `:152` 行为一致
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 5-未修复-低-新增错误路径缺少自动化测试覆盖
- **入口/函数**: `tests/test_repair_codex_reasoning_history.py`
- **文件（行号）**: 新增测试在 `:31-100`；被测代码 `scripts/repair-codex-reasoning-history.py:111-139`、`scripts/agent-tools.zsh:462-479`
- **输入场景**: 以下边界目前只靠本次 review 的手工探针验证，无回归测试：launcher 参数个数 ≠2（rc 2 + 用法）；repair 工具不在 PATH（rc 1，不触碰会话）；business 分支（`target_config=config.toml`、不注入 `-p`）；rollout 0 个/2 个匹配；非规范 UUID（大写/无连字符/带花括号）；target config 缺 `model` 键 / 是符号链接；keep 模式下 reasoning 无前置 `turn_context` 的 fail-closed；重复 `--resume` 幂等（备份数不变）
- **实际分支**: 上述路径均由 `parser.error`（rc 2）或 launcher 显式 `return 1/2` 覆盖，行为正确
- **预期行为**: 关键边界有自动化回归测试
- **实际行为**: 现有 3 个新测试覆盖核心 happy path、普通 resume 不变性、非法 id 和 sync 安装；其余边界回归时不会被发现
- **直接证据**: 本次 review 的 17 项脚本级探针与 8 项 launcher 探针全部 PASS（隔离环境，stub codex + 假 HOME）；测试文件 `:45-89` 的 launcher 测试未覆盖上述分支
- **影响**: 回归发现延迟；非当前正确性缺陷
- **建议改法和验证点**: 把探针中参数个数、工具缺失、business 分支、0/2 匹配、非规范 UUID、keep 模式 orphan reasoning 六类固化为测试（现有测试的 fake-HOME/stub 脚手架可直接复用）
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

- OpenAI 原生跨模型 resume（如 `gpt-6-astra` → `gpt-6-sol`，同账号）：OpenAI 端点是否接受源模型的 encrypted reasoning（item ID 在其 store 中）？keep 模式一律丢弃非目标模型的 reasoning——保守且安全，但若同族跨模型本可保留，则是不必要的推理连续性损失。需 live 验证；当前文档已明确丢弃语义，不构成 defect。
- 更老版本 codex 写的会话是否可能存在缺 `model` 键的 `turn_context`？若存在，keep 模式会 fail-closed 中止修复（`repair-codex-reasoning-history.py:43-44`），变通路径是位置参数 + `--drop-reasoning-model` 模式。未检查真实会话（任务约束）。
- 以第三方模型为目标的端到端 resume（如 `kimi_codex --resume` 修复 gpt 会话后真实发起请求）只验证到 argv 构造层（stub 捕获）；早前裁决仅对 OpenAI 目标做过 ephemeral resume 实证（`turn.completed`）。第三方网关对修复后历史的接受度未实证。

## Residual Risk

- **idle-open-client inode 交换**: `os.replace`（`repair-codex-reasoning-history.py:173`）把路径原子换到新 inode；持有旧 fd 的 Codex 客户端会继续往失联 inode 追加，新 turn 静默丢失。`:162-172` 的双重 stat+内容校验能抓到活跃写入者，抓不到“打开但空闲”的客户端。缓解靠 README 的“关闭其他 Codex 客户端”指示，属人工约束。
- **TOCTOU 残余窗口**: 最后一次内容校验（`:169-172`）与 `os.replace`（`:173`）之间仍有极小窗口，设计上接受。
- **live 安装漂移（review 时点）**: `~/.config/zsh/agent-tools.zsh` 与仓库源不一致，`~/.local/bin/repair-codex-reasoning-history.py` 尚不存在——与文档“先 sync 再开新 shell”一致；未同步时 launcher 以 rc 1 + 明确提示 fail-closed（探针 3 证实）。merge 后需执行 `sync-agent-tools.sh` 该功能才生效。
- `--session-id` 与 `--drop-reasoning-model` 组合被允许但无测试/文档（两过滤器取并集，行为无害）。
- 早前裁决遗留：跨 provider resume 后首个请求的真实路由在加锁会话上未实证（见 `docs/reviews/resume-model-adjudication-20260925-100512.md`），本次改动不改变该结论。

---

## 测试与 canary 证据（简述）

- 测试：`python3 -m unittest discover -s tests -p 'test_*.py'` 全量 **20/20 PASS**（Python 3.11.15，含新增 3 项：keep/drop 语义、launcher 端到端 stub 测试、sync 安装测试）；`git diff --check` 干净。
- 隔离探针（全部在 `$TMPDIR` 下，stub codex + 假 HOME，未触碰真实会话/安装）：脚本级 17/17 PASS（大写/无横线/花括号 UUID 拒绝、位置参数冲突、缺 `--sessions-root`/`--target-config`、0/2 匹配、config 缺 model/符号链接、sessions root 缺失、dry-run/apply/幂等、orphan reasoning fail-closed、drop 模式旧行为保持、空 id）；launcher 级 8/8 PASS（1/3 参数 rc 2 且 codex 未被调用、工具缺失 rc 1、`--title` 前后位置均可、重复 `--resume` 不新增备份、`--resume=` 旁路、business 分支目标配置与无 `-p` 注入均正确）。
- 真实 CLI 核对：codex-cli 0.156.1 `resume` 支持 `-p/--profile`（`Layer $CODEX_HOME/<name>.config.toml`），与 launcher 注入顺序 `resume -p <id> <session>` 一致；无全局 `--resume`（支撑 Finding 1）；`resume` 有 `[PROMPT]` 位置参数（支撑 Finding 3）；business `~/.codex-agent/business/config.toml` 第 1 行存在顶层 `model = "gpt-6-sol"`（business 修复路径可解析）。
- Canary：已用工具读取指定 canary 文件；总控比对通过，token 不写入仓库。
- 结论：5 项 finding 均为低severity（2 项参数边界 UX、1 项 Python 版本依赖声明、1 项文档精度、1 项测试缺口），无中高severity correctness/stability 问题；核心链路（修复→私有备份→原子替换→`codex resume -p`）与错误路径均 fail-closed 且有直接证据支撑。
