RUNTIME/PROVIDER/MODEL: claude/mimo/mimo-v2.6-pro[1m]
CANARY=mimo-76a97c3d

# Code Review

## Scope

- Mode: current changes
- Branch or PR: `fix/shared-provider-resume`（工作区未提交改动；`git diff main...HEAD` 为空，全部改动为 unstaged/untracked）
- Base: `main`
- Output file: `docs/reviews/code-review-20260925-084205.md`
- Included scope:
  - `codex-agent/model-providers.toml`（新增，9 个共享 provider 定义）
  - `scripts/sync-codex-providers.py`（新增，带标记块原子同步器）
  - `scripts/sync-codex-agent.sh`（接入 registry 同步，先于卡片部署）
  - `scripts/compose-codex-app-config.py`（桌面 app 配置合成改为注入共享 registry、拒绝卡片定义 provider）
  - `codex-agent/profiles/{ds-flash,glm,glm-flash,kimi,local,mimo,mimo-fast,mimo-flash,qwen}/config.toml`（移除 `[model_providers.*]`；glm-flash/mimo-fast/mimo-flash 改用独立 provider ID）
  - `tests/test_provider_registry.py`（新增，4 个用例）
  - `README.md` / `README.zh.md`（对应文档更新）
  - 关联走读（未改动但为契约面）：`scripts/agent-tools.zsh`（`_codex_agent_app` 桌面启动链、`_codex_agent_launch` 的 `-p`/resume 接线、`_codex_agent_key_name`/`_codex_agent_shim_port` 映射）、`codex-agent/shim-routes.json`（端口契约）
- Excluded scope:
  - 凭据与会话内容（按约束不读取）；`~/.codex/config.toml` 仅做存在性/计数 grep（确认无未标记 `model_providers`、无 managed marker），未读取正文
  - `~/.codex-agent/business`（独立 home，不在本改动管理范围）
  - Codex 二进制内部实现（resume 解析 provider 的内部机制无法从仓库验证，见 Open Questions）
  - `skills/`、`codex-agent/bin/`（本 diff 未触及）
- Parallel review coverage: 无（按任务约束不派发 sub-agent；全部走读由主 reviewer 完成）

## Findings

### 1-未修复-中-升级窗口内 compose 硬拒绝旧模型卡，`*_codex_app` 启动崩溃
- **入口/函数**: `scripts/agent-tools.zsh` 的 `_codex_agent_app` → `compose-codex-app-config.py` `main()`（app 每次启动都会调用）
- **文件(行号)**: `scripts/compose-codex-app-config.py:88-90`（raise 路径）；调用点 `scripts/agent-tools.zsh:399-401`；README 描述的同步顺序 `README.md:549-557`（`sync-agent-tools.sh` 在 `sync-codex-agent.sh` 之前）
- **输入场景**: 用户按 README 顺序升级：先跑 `./scripts/sync-agent-tools.sh`（安装新版 `compose-codex-app-config.py` 与 `agent-tools.zsh`），此时磁盘上的 `~/.codex/<id>.config.toml` 仍是旧卡（含 `[model_providers.*]`，例如旧 kimi 卡带 `[model_providers.kimi]`）；随后在跑 `./scripts/sync-codex-agent.sh` 之前启动任一桌面 profile（如 `kimi_codex_app`）
- **实际分支**: `card_sections` 命中 `name.startswith("model_providers.")` → `raise ValueError`
- **预期行为**: 运行时合成器对过渡期的旧卡要么容错（忽略/告警卡内 provider 段，以共享 registry 为准），要么给出可行动的单行错误；仓库侧不变量（卡片不得定义 provider）应由仓库结构与测试保证，而不是在用户机器上以崩溃方式执法
- **实际行为**: 未捕获的 `ValueError` 以 Python traceback 退出（rc=1），`|| return 1` 使 app 不启动。实测：`ValueError: model card must not define shared provider model_providers.kimi`，且 `--out` 未被写入（fail-closed，但用户可见故障）
- **直接证据**: `compose-codex-app-config.py:88-90` 无条件 raise；`_codex_agent_app` 在每次 launch 调用 compose（`agent-tools.zsh:393-402`）；README 的 Maintenance 顺序先 `sync-agent-tools.sh` 后 `sync-codex-agent.sh`（`README.md:549-557`），即文档顺序本身制造该窗口；另 `sync-codex-agent.sh:32-34` 若 registry 同步失败（`set -euo pipefail`，`sync-codex-agent.sh:2`）则卡片不部署，旧卡残留使窗口持续。次要同源问题：`compose-codex-app-config.py:76` 现在无条件读取 `--base`，base 缺失时即使 `--out` 已存在也以 `FileNotFoundError` traceback 崩溃（实测 rc=1），旧版在 out 存在时不读 base
- **影响**: 用户可见的功能不可用（桌面 app 无法启动），升级序列或 sync 中断即可触发；错误形态是 traceback 而非诊断信息
- **建议改法和验证点**: 将卡内 `model_providers.*` 视为 legacy 输入：跳过并打 stderr 告警（共享 registry 为准），仅在 `--strict` 或测试模式下 raise；或在 `_codex_agent_app` 调用前预检卡片并给出“先跑 scripts/sync-codex-agent.sh”的提示。验证点：用旧卡（含 `[model_providers.kimi]`）+ 新 compose 跑通 app 合成并得到含 9 个 provider 的 out；缺失 base 时返回单行错误而非 traceback
- **修复风险（低/中/高）**: 低（局部放宽一个判断分支）
- **严重程度（低/中/高/严重）**: 中

### 2-未修复-中-tests 未证明本改动引入的失败路径，跨文件 provider 契约无锁定
- **入口/函数**: `tests/test_provider_registry.py`（全部 4 例）；被测面 `sync-codex-providers.py` `main()` 写路径、`compose-codex-app-config.py` 失败分支、registry ↔ `shim-routes.json` ↔ `agent-tools.zsh` 映射的一致性
- **文件(行号)**: `tests/test_provider_registry.py:25-63`；空白面：`sync-codex-providers.py:27-28`（marker 损坏/重复拒绝）、`sync-codex-providers.py:65-76`（原子写 + 中途修改检测）、`compose-codex-app-config.py:88-90`（卡定义 provider 拒绝）、`sync-codex-providers.py:22-24`（registry 形状校验）
- **输入场景**: 未来任何改动触碰这些失败分支或跨文件契约时
- **实际分支**: 现有测试只覆盖 happy path：fresh 安装 + 幂等、`[projects]` 保留、unowned conflict 拒绝、app home 刷新 4 条路径
- **预期行为**: 该 fix 的核心风险面——(a) marker 不完整/重复/倒置时拒绝且不写坏 base；(b) 同步中途 base 被并发修改时拒绝替换；(c) 旧卡含 provider 时 compose 的行为被测试固定；(d) registry 的端口/env_key 与 `shim-routes.json`、`_codex_agent_key_name`/`_codex_agent_shim_port` 一致——应有回归测试锁定
- **实际行为**: 上述 (a)(b)(c)(d) 全部无测试。实测确认这些分支确实存在且行为各异：marker 倒置报出令人费解的 `ValueError: not enough values to unpack`（`sync-codex-providers.py:30-32` 的 split 解包）；`main()` 的 mkstemp/fchmod/fsync/TOCTOU 重读/`os.replace`（`sync-codex-providers.py:65-76`）完全未被测试执行（测试只 import `compose()`）；卡带 provider 的 compose 拒绝路径无断言；`test_provider_registry.py:32-34` 仅断言 3 对 `base_url` 互不相等，端口与 `shim-routes.json`（8788/8789/8790/8791/8792/8793/8794/8795）及 `agent-tools.zsh:272-303` 映射的一致性无人校验（本次人工核对全部一致，但契约跨 3 个文件、含重复真源，registry 改端口不会被任何测试发现）
- **直接证据**: `python3 -m unittest discover -s tests -v` → 4 tests OK，全部为 happy path；失败分支探针（本次实测）证明 (a)(c) 存在真实行为且未被固定；无 CI（仓库无 `.github`）、README 无测试运行入口，测试实际处于无人执行状态
- **影响**: 回归防护缺口——本 fix 修复的 resume 故障若被未来改动重新引入（例如卡片再次携带 provider、registry 端口漂移、marker 写坏 base），测试全绿仍会坏；失败路径行为漂移不可见
- **建议改法和验证点**: 补 4 组测试：marker 损坏/倒置/重复 → `ValueError` 且 base 原文不变；`main()` 对并发修改的 base → 非零退出且不替换（可注入假 clock/或直接断言重读比较）；compose 对卡带 provider 的期望行为（按 finding 1 的裁决固定为容错或报错）；registry ↔ `shim-routes.json` 端口与 registry ↔ `_codex_agent_key_name` env_key 的一致性断言（可解析 zsh 的 case 表或把映射提取为数据文件）。并在 README/CI 增加 `python3 -m unittest discover -s tests` 入口
- **修复风险（低/中/高）**: 低（纯测试与可测性抽取）
- **严重程度（低/中/高/严重）**: 中

### 3-未修复-低-registry 校验接受 `[model_providers]` 下的非表值，坏 registry 会写入 live base
- **入口/函数**: `sync-codex-providers.py` `compose()` 的 registry 形状校验
- **文件(行号)**: `scripts/sync-codex-providers.py:22-24`
- **输入场景**: `codex-agent/model-providers.toml` 被手工编辑后混入非表值（例如 `[model_providers]` 表下写 `oops = 1`，或某 provider 表写错层级）
- **实际分支**: `set(registry) != {"model_providers"} or not registry["model_providers"]` 仅校验顶层键集合与非空；`managed_ids = set(registry["model_providers"])` 把标量键也当作 provider ID
- **预期行为**: 按报错文案“registry must contain only nonempty [model_providers.*] tables”，每个值必须是表；非法 registry 应拒绝
- **实际行为**: 实测 `[model_providers]\noops = 1\n\n[model_providers.kimi]\n...` 被接受，`oops = 1` 原样进入合成结果并写入 live base（`compose` 尾部的 `tomllib.loads(result)` 只保证 TOML 合法，不保证 schema）。Codex 解析含标量的 `model_providers` 表的行为不可控，最坏使共享 home 的 config 整体不可用（所有 profile 受影响）
- **直接证据**: `sync-codex-providers.py:23-25` 只做顶层集合与真值检查；探针输出 `non-table-value registry: ACCEPTED, result contains 'oops': True`。对照：顶层杂散键（`foo = 1`）会被正确拒绝，说明校验意图存在但覆盖不完整
- **影响**: 人工改坏 repo 文件后 sync 静默把坏 schema 写入机器 live 配置；故障延迟到 Codex 启动才暴露，且影响全部 profile
- **建议改法和验证点**: 在 `compose()` 增加 `all(isinstance(v, dict) and v for v in registry["model_providers"].values())` 断言（并考虑拒绝裸 `[model_providers]` 标量键）；补一条负例测试
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 4-未修复-低-删除卡片时丢失直连 fallback URL，停用 shim 的 runbook 不再可执行
- **入口/函数**: 9 张卡的 `[model_providers.*]` 段删除；`codex-agent/model-providers.toml` 未收录对应注释
- **文件(行号)**: 删除处见 diff（如 `codex-agent/profiles/qwen/config.toml:14-21` 旧文、`codex-agent/profiles/ds-flash/config.toml:14-21` 旧文）；新 registry `codex-agent/model-providers.toml:5-65` 无任何直连注释
- **输入场景**: 运维需要停用 shim、改走直连原值（旧注释明确的操作：“停用 shim 时改回本行”）
- **实际分支**: 旧卡每项都带注释行 `# base_url = "https://…/v1" # 直连原值；停用 shim 时改回本行`；本次 diff 将其随段整体删除
- **预期行为**: 迁移到 registry 时把操作所需的直连 URL 一并迁移（registry 注释即可），或在 README 记录等价 runbook
- **实际行为**: 带路径后缀的直连 base_url（`/api/v1`、`/coding/v1`、`/compatible-mode/v1` 等）只存在于 git 历史。`shim-routes.json` 只有 host 级 `upstream`（如 `https://dashscope.aliyuncs.com`，无 `/compatible-mode/v1`），无法唯一还原旧值；`local` 的注释 shim 路由说明也一并删除（`README.md:281` 改写）
- **直接证据**: 对 HEAD 版卡片 `git show` 可见旧注释行；新 registry 全文无 `直连`/`# base_url` 注释；`shim-routes.json` upstream 均无路径后缀
- **影响**: 可维护性/运维知识丢失——停用 shim 的既有操作流程无法从当前仓库内容完成，只能翻 git 历史；纯文档性损失，不影响当前运行正确性
- **建议改法和验证点**: 在 `model-providers.toml` 各表恢复一行注释直连 URL（含路径），或在 README 增加“停用 shim”小节列出 9 条直连地址
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

- resume 修复的前提机制（“base config 中存在历史 provider，`codex resume`/`-p` 才能解析会话中记录的 provider”）无法从仓库内验证：它依赖 Codex 闭源的 config 分层与 thread provider 解析行为。本 review 按 README/分支声明接受该前提，但未独立证实。若前提不成立，整个改动的收益面需要重新评估。
- `compose-codex-app-config.py` 的 `--out` 是否在任何调用场景下可能与 `--base` 指向同一文件（自合成）？当前调用点只有 `agent-tools.zsh:399-400`（out 为 app-home，base 为共享 home），未发现自合成路径；若有外部调用者，marker 重排行为需要再验证。
- `sync-codex-providers.py` 要求 base 配置必须已存在（`sync-codex-providers.py:52-54`），全新机器（Codex 从未写过 `~/.codex/config.toml`）上 `sync-codex-agent.sh` 会整体失败。是有意的“不代管机器初始化”，还是应创建空 base？需要 owner 裁决。

## Residual Risk

- **旧变体线程的静默换模型**：legacy `glm`/`mimo` ID 无法还原创建变体（README.md:257-261 已披露），但失败模式是静默的：旧 glm-flash 线程（provider `glm`@8795）resume 后解析到 8789，且 shim 会按 `shim-routes.json` 的 `to` 改写模型名（`glm-5.3`），对话将以错误变体继续而不报错。mimo-flash/mimo-fast 同理（→ `mimo-v2.6-pro`）。建议在 release note/README 明确“旧变体线程 resume 会落到普通路由”，必要时引导用户对关键线程固定 resume 用卡。
- **原子性残余**：`sync-codex-providers.py:65-76` 使用同目录 mkstemp + fchmod（保留权限）+ fsync + 重读比较 + `os.replace`，对读者是原子替换，方向正确；但 (a) 重读比较与 replace 之间的 TOCTOU 窗口仍可能吞掉并发写入；(b) rename 后未 fsync 目录，掉电极端场景下 rename 本身不保证持久；(c) SIGKILL 会在 `~/.codex/` 留下 `.config.toml.*` 孤儿临时文件。对本用途可接受，未列为 finding。
- **失败诊断质量**：managed marker 倒置（END 在 START 前）时报 `not enough values to unpack (expected 2, got 1)`（实测），虽 fail-closed 但难以定位；与 finding 3 同属校验/诊断面，未单列。
- **合成产物残留**：(a) app-home out 中已不在 registry 的旧 provider 表会永远保留（实测 `gone` 表存活），registry 删 ID 后 app home 不收敛；(b) 共享 base 的 `# END` marker 注释会漏进合成后的 app-home 配置而 `# BEGIN` 丢失（实测），TOML 合法、纯外观问题；(c) `compose-codex-app-config.py` 不校验输出 TOML 合法性（既有问题，本 diff 未扩大）。
- **测试执行无入口**：仓库无 CI、README 未写测试命令；4 个测试当前通过（本次实测），但依赖人工记得执行（并入 finding 2 的修复面）。
- **未检查面**：Codex 二进制对重复 provider ID 跨层（base vs card）的实际合并语义、`business` home、shim 服务实际转发行为、真实 resume 端到端（涉及会话数据，按约束不触碰）；README “Edit only the source files” 清单仍缺 `compose-codex-app-config.py` 等（既有问题，本 diff 仅新增了两行）。

## Validation Performed

- diff 走读：`git diff`（unstaged 全量）+ untracked 全文阅读（`model-providers.toml`、`sync-codex-providers.py`、`tests/test_provider_registry.py`）；`git diff main...HEAD` 为空（改动全在工作区，未提交）。
- TOML 有效性：registry + 12 张 profile 卡全部 `tomllib` 解析通过；卡片内已无任何 `[model_providers.*]`；provider ID 共 9 个且互不重复（`tomllib` 对重复表会报错，双重保险）。
- 跨文件端口/env_key 契约人工核对一致：registry base_url 端口 ↔ `shim-routes.json`（8788 deepseek / 8789 glm / 8790 kimi / 8791 mimo / 8792 qwen / 8793 mimo_flash / 8794 mimo_fast / 8795 glm_flash）↔ `_codex_agent_shim_port`；registry env_key ↔ `_codex_agent_key_name`（含 `local` 无 key、gpt-6 系走 `openai`）。
- 迁移兼容：本机 `~/.codex/config.toml` grep 计数确认 0 处 `model_providers`、0 处 managed marker → 首次 sync 走全新安装路径，不会触发 unowned conflict；新 ID（`glm_flash`/`mimo_fast`/`mimo_flash`）与旧卡端口逐一对照（8795/8794/8793），`glm`/`mimo` 保留原路由（8789/8791）与 README 披露一致。
- 行为探针（`$TMPDIR` 内，不触碰仓库与 live 配置）：marker 倒置/单侧/重复均拒绝；unowned provider 与块外内容（含块后 `[projects]`）保留且 marker 保持 1/1；非法 base TOML 拒绝；非表 registry 值被接受（finding 3）；旧卡+新 compose → rc=1 traceback 且 out 不变（finding 1）；缺 base+有 out → FileNotFoundError（finding 1）；`# END` 泄漏进 app-home（residual）。
- 测试：`python3 -m unittest discover -s tests -v` → 4 tests OK（覆盖 fresh 安装、幂等、conflict 拒绝、app-home 刷新 4 条 happy path）。
- 语法：`bash -n scripts/sync-codex-agent.sh`、`py_compile` 两个 Python 脚本均通过。
