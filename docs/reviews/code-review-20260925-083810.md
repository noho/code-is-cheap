# Code Review

## Scope

- Mode: current changes
- Branch or PR: `fix/shared-provider-resume`（无领先 main 的提交；`git merge-base main HEAD` = `4aa780f` = HEAD，全部改动为未提交的 working tree 改动 + 3 个 untracked 路径）
- Base: `main`（`4aa780fb606154c5ff7acda3d94d5bbda1cb7c63`）
- Output file: `docs/reviews/code-review-20260925-083810.md`
- Included scope:
  - 新增：`codex-agent/model-providers.toml`、`scripts/sync-codex-providers.py`、`tests/test_provider_registry.py`
  - 修改：`scripts/sync-codex-agent.sh`、`scripts/compose-codex-app-config.py`、`codex-agent/profiles/{ds-flash,glm,glm-flash,kimi,local,mimo,mimo-fast,mimo-flash,qwen}/config.toml`、`README.md`、`README.zh.md`
  - 调用链追踪：`scripts/agent-tools.zsh`（`_codex_agent_launch` 的 `-p` 注入与 resume 路径、`_codex_agent_app` 每次启动 compose、`_codex_agent_require_home`、`_codex_agent_shim_port`）、`codex-agent/shim-routes.json`、`codex-agent/bin/codex-auto-review-shim`（确认 `to` 只改写 guardian 模型 id）、`scripts/codex-agent-run`
  - 实机状态证据：`~/.codex/config.toml` 当前无 `[model_providers.*]` 且无标记；`~/.codex/*.config.toml` 共 9 张已安装卡当前全部仍是旧格式（含内嵌 `[model_providers.*]`）
- Excluded scope: `scripts/patch-codex-model-catalog.py`、`codex-agent/bin/*` 本体（未改动）；session 会话内容（任务禁止）；凭证文件（任务禁止）
- Parallel review coverage: 无（任务禁止派发 sub-agent，全部由主 reviewer 走读）

改动意图（从 diff + README + 注释归纳）：把分散在各模型卡里的 `[model_providers.*]` 网关定义集中到仓库所有的注册表 `codex-agent/model-providers.toml`，由 sync 以带注释标记的块写入共享 base `~/.codex/config.toml`，使 `codex resume`/`exec resume` 在换卡续会话时能解析历史 provider；同时为 glm-flash/mimo-fast/mimo-flash 引入独立 provider ID（`glm_flash`/`mimo_fast`/`mimo_flash`），桌面 app composer 改为从共享 base 复制 provider 段并拒绝含 provider 段的卡。

## Findings

### 1-未修复-中-base config 缺失时整个 sync 中止，main 上同场景可正常安装
- **入口/函数**: `scripts/sync-codex-agent.sh` 主流程 → `scripts/sync-codex-providers.py:main`
- **文件(行号)**: `scripts/sync-codex-agent.sh:2`（`set -euo pipefail`）、`scripts/sync-codex-agent.sh:32-34`（providers 同步置于卡片循环之前）、`scripts/sync-codex-providers.py:53-54`（`if not base.is_file(): ap.error(...)`）
- **输入场景**: 全新机器（Codex 从未运行、`~/.codex/config.toml` 尚不存在）、base config 被删/损坏后重装、或 `CODEX_SHARED_HOME` 指向新目录时运行 `./scripts/sync-codex-agent.sh`
- **实际分支**: `sync-codex-providers.py:53-54` → `ap.error` → exit 2 → 外层 `set -e` 立即中止
- **预期行为**: sync 是仓库声明的安装入口（README「Sync the Codex agent profiles…」），base 缺失时应跳过注册表同步或创建最小 base，继续安装卡片/shim/路由——main 上该场景卡片、shim、路由、目录全部正常安装
- **实际行为**: 实测（`CODEX_AGENT_TARGET`/`CODEX_SHARED_HOME` 指向 tmp 空 home 跑真实脚本）：exit 2，输出 `error: base config does not exist`，卡片 0 张安装、bin/ 未创建、shim-routes.json 未安装，只留下一个空的 `model-catalogs/` 目录
- **直接证据**: `sync-codex-providers.py:53-54` 硬错误；`sync-codex-agent.sh:32-34` 位于卡片循环（:37-50）、bin 安装（:54-58）、路由安装（:61）之前；`set -euo pipefail`（:2）使 exit 2 直接终止脚本
- **影响**: 静默失效型回归——fresh/broken-base 机器上 sync 完全不可用，且失败点在所有其它安装步骤之前，用户得到的是「什么都没装」
- **建议改法和验证点**: base 缺失时创建空文件（或仅含注册表块的新 base）并继续；或在 sync-codex-agent.sh 中 `: > "$shared_home/config.toml"` 守护。验证点：tmp 空 home 跑 sync 应安装全部卡片且 exit 0
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 2-未修复-中-composer 对旧格式卡抛未捕获 ValueError，拉取分支后未 re-sync 的窗口内所有桌面 app 启动失败
- **入口/函数**: `scripts/agent-tools.zsh:_codex_agent_app`（每次 `open` 前都 compose）→ `scripts/compose-codex-app-config.py:main`
- **文件(行号)**: `scripts/compose-codex-app-config.py:88-90`（`raise ValueError(f"model card must not define shared provider {name}")`）、`scripts/agent-tools.zsh:399-400`（`compose-codex-app-config.py ... || return 1`）
- **输入场景**: 用户拉取本分支后尚未运行 `sync-codex-agent.sh`（已安装卡仍是旧格式），启动任一 managed 桌面 app 实例（如 `kimi_codex_app`）
- **实际分支**: 旧卡含 `[model_providers.kimi]` → `split_sections` 捕获 → :88-90 raise → traceback → exit 1 → launcher `return 1`，app 不启动
- **预期行为**: 迁移窗口内 composer 对旧卡降级处理（warn + 忽略卡内 provider 段，共享 base 为准），或至少给出可操作错误提示（「请重跑 sync-codex-agent.sh」）
- **实际行为**: 实测用旧格式卡跑新 composer：完整 Python traceback `ValueError: model card must not define shared provider model_providers.kimi`，exit 1。实机证据：当前 `~/.codex/` 下 9 张已安装卡（ds-flash/glm/glm-flash/kimi/local/mimo/mimo-fast/mimo-flash/qwen）全部仍是含 `[model_providers.*]` 的旧格式——即本工作区当前状态下启动任何 managed app 必现
- **直接证据**: `compose-codex-app-config.py:88-90` 的 raise 无 try/except 包裹（对比 `sync-codex-providers.py:58-59` 用 `ap.error` 干净退出）；`agent-tools.zsh:399-400` 每次启动都执行 compose；`grep -l '^\[model_providers\.' ~/.codex/*.config.toml` 命中全部 9 张卡
- **影响**: 仅迁移窗口内的局部行为错误（跑一遍 sync 即恢复），但失败形式是 traceback、无指引，且 composer 版本与已安装卡版本被硬绑定（overcoupling：两个独立部署的产物必须同批更新）
- **建议改法和验证点**: 把 raise 改为「忽略卡内 provider 段 + stderr 警告提示 re-sync」，或捕获后 `ap.error` 风格干净退出并指明修复命令。验证点：旧卡 + 新 composer 启动 app 成功且输出警告
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

### 3-未修复-中-legacy flash/fast 会话记录的共享 provider ID（`glm`/`mimo`）在新注册表中解析到普通变体网关与不同凭证
- **入口/函数**: `codex exec -p <variant> resume <旧会话>` / `codex resume` 的 session-resume bootstrap（agent-tools.zsh:472-482 把 `-p <id>` 注入 resume 路径）；设计前提见 `sync-codex-agent.sh:30-31` 与 `model-providers.toml:1-3`：「a resumed thread may need to resolve any earlier provider before the newly selected model card takes effect」
- **文件(行号)**: `codex-agent/model-providers.toml:39-44`（`mimo` → 8791 + `MIMO_PLAN_API_KEY`）、:12-17（`glm` → 8789）；对照 main 上旧卡（`git diff` 删除段）：mimo-fast 旧卡内嵌 `[model_providers.mimo]` → 8794 + `MIMO_API_KEY`、mimo-flash 旧卡 → 8793 + `MIMO_PLAN_API_KEY`、glm-flash 旧卡 `[model_providers.glm]` → 8795
- **输入场景**: 本分支生效前保存的 mimo-fast / mimo-flash / glm-flash 会话（recorded provider 分别为共享 ID `mimo`/`mimo`/`glm`），在本分支下用同变体卡 resume，例如 `-p mimo-fast` resume 一条旧 mimo-fast 会话
- **实际分支**: resume bootstrap 解析 recorded provider `mimo` → 命中新注册表 `mimo`（8791，token-plan 上游，`MIMO_PLAN_API_KEY`）而非创建时的 8794（api.xiaomimimo，`MIMO_API_KEY`）；`glm-flash` 旧会话的 `glm` → 8789 而非 8795
- **预期行为**: 按本分支自身的设计目标（「旧 provider 在 resume 时可解析」），解析结果应对应该会话创建时的网关语义
- **实际行为**: ID 被复用到不同网关/不同 env_key。main 上同卡 resume 旧会话端到端正确（卡内自带正确映射），异卡 resume 是响亮失败（provider 不存在）；本分支把前者变成解析到错误网关，把后者变成静默可解析。若 Codex 在 bootstrap 阶段用 recorded provider 发任何请求，即错上游 + 错凭证（上游报错或错配额计费）
- **直接证据**: 注册表与 shim-routes.json 端口一一对应（已程序化交叉验证：8788/8789/8790/8791/8792/8793/8794/8795 全部命中路由）；`mimo` 与 `mimo_fast`/`mimo_flash` 的上游分别为 token-plan-cn.xiaomimimo.com 与 api.xiaomimimo.com（shim-routes.json），env_key 分别为 `MIMO_PLAN_API_KEY` 与 `MIMO_API_KEY`。README.md:256-259 明确披露该取舍：「Legacy sessions recorded with the shared `glm` or `mimo` IDs cannot identify which variant created them; those IDs retain the normal `glm` and `mimo` routes」——即作者已知并接受，但「已文档化」不消除行为回退
- **影响**: 错误上游/错误凭证的请求（若 bootstrap 发请求）或仅验证性解析（则无实际请求差异）——影响面取决于 Codex 内部 resume 行为，见 Open Questions
- **建议改法和验证点**: 短期无法无损修复（旧会话的 recorded ID 本身不含变体信息）；可考虑在注册表注释/README 中给出一次性迁移指引（如用同变体卡 resume 前先确认 shim 8791/8789 对旧模型 id 的容忍度），或在 release note 中明示「旧 flash/fast 会话首次 resume 建议用创建时的变体卡」。验证点：用 `-p mimo-fast` resume 一条 pre-branch mimo-fast 会话，观察 bootstrap 是否对 8791 发请求及其响应
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 中

### 4-未修复-低-managed block 的所有权判定押在可丢失的 TOML 注释标记上；若 Codex/桌面 app 全量重写 config.toml 丢注释，下一次 sync 误判 unowned conflict 并整体中止
- **入口/函数**: `scripts/sync-codex-providers.py:compose`
- **文件(行号)**: `sync-codex-providers.py:17-18`（START/END 为注释字符串）、:27-34（按注释切出 `outside`）、:36-39（`outside` 中与 managed IDs 重叠即 raise）
- **输入场景**: 共享 base `~/.codex/config.toml` 是 Codex 本体与桌面 app 会写回的文件（README.md 自述 app 写入 `[mcp_servers.*]`/`[plugins.*]`/`[marketplaces.*]`/`[tui.*]`/`[desktop]` 等）。若某次写回走「解析→重序列化」路径，注释（含 BEGIN/END 标记与注册表头注释）被丢弃，但 `[model_providers.*]` 表保留
- **实际分支**: 下一次 sync：:27 计数为 0 个标记 → `START not in base_text` → `outside = base_text` → :36-39 发现 `kimi` 等 ID 已存在 → raise「provider IDs already owned outside managed block」→ 整个 sync 中止（同 finding 1 的放大效应）
- **预期行为**: 所有权标记应能 survive 配置文件的常规重写（例如用一个 managed 专用根键/ sentinel 表，或把注册表放独立文件由 base `include`——若 Codex 支持）
- **实际行为**: 标记丢失后 sync 从「幂等刷新」退化为「永久 conflict 中止」，且报错文案（already owned）会误导用户以为自己有手工冲突
- **直接证据**: :17-18 标记是注释；TOML 重序列化丢注释是标准行为；README.md:256-263 自述该文件由 app 共同写入。Codex/app 是否真的全量重写丢注释未能从仓库证实 → Open Questions
- **影响**: 条件触发的不可恢复 sync 中止，需手工清理 base 中的 provider 表才能恢复
- **建议改法和验证点**: 验证 Codex 桌面 app 写回 config.toml 是否保留注释（改 [desktop] 状态后 diff）；若丢注释，改用结构性标记（如 `[model_providers._managed]` sentinel 或独立 registry 文件）。验证点：app 写回后重跑 sync 仍幂等
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 低

### 5-未修复-低-并发写保护存在残余 TOCTOU 窗口，且 os.replace 会静默把 symlink 化的 base 变成普通文件
- **入口/函数**: `scripts/sync-codex-providers.py:main` 原子写路径
- **文件(行号)**: `sync-codex-providers.py:56`（首次读）、:65-71（写 tmp + fsync）、:72-73（重读比对）、:74（`os.replace`）
- **输入场景**: (a) sync 运行期间桌面 app/ Codex 恰好写回同一 config.toml（并行配置写入——该文件按设计就是共治文件）；(b) 用户用 dotfiles 把 `~/.codex/config.toml` 做成 symlink
- **实际分支**: (a) :72 的重读检查只能发现 :56 到 :72 之间的写入；:72 通过到 :74 replace 之间的写入被静默覆盖丢失。(b) `os.replace(tmp_name, base)` 用普通文件替换 symlink 本身，dotfiles 链接被无声拆除（对比 compose-codex-app-config.py:94 的 `write_text` 跟随 symlink，两者行为不一致）
- **预期行为**: (a) 无锁前提下至少把窗口文档化；(b) 检测到 base 是 symlink 时拒绝或解析到真实路径后替换
- **实际行为**: (a) 窗口微秒级但真实存在，丢失的是 app 写入的信任/状态条目；(b) symlink 被静默替换
- **直接证据**: :72-74 检查与替换非原子；:74 `os.replace` 语义；:53 `base.is_file()` 对 symlink 为真
- **影响**: 小概率状态丢失 / 用户布局被静默改变
- **建议改法和验证点**: (b) `base.is_symlink()` 时 `ap.error`；(a) 可接受现状但在 docstring 注明。验证点：symlink base 跑 sync 被拒绝
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 6-未修复-低-从注册表移除/重命名的 provider 在已组合的 app home 中永久残留（无删除路径）
- **入口/函数**: `scripts/compose-codex-app-config.py:main` 的 provider 复制循环
- **文件(行号)**: `compose-codex-app-config.py:85-87`（仅 `base_sections[name] = body` 覆盖/新增，从不删除）
- **输入场景**: 未来从 `model-providers.toml` 移除或重命名某 provider（本次 rename glm→glm_flash 即是先例），已组合过的 app home（`~/.codex-agent/app-data/<id>/home/config.toml`）里对应的旧 provider 段
- **实际分支**: shared_sections 不再含该 ID → dest 中的旧段原样保留，仍以旧端口/旧 env_key 可解析
- **预期行为**: app home 中仓库所有的 provider 集合应与注册表对账（覆盖 + 删除失效项），与 sync-codex-providers 对 base 的「整块替换」语义一致
- **实际行为**: 实测：dest 预置 `[model_providers.retired]`（不在注册表），compose 后该段完整存活。旧网关在未来 resume 中静默存活，与 finding 3 同类的语义漂移延长线
- **直接证据**: :85-87 只有赋值无删除；实测输出含 `retired` 段
- **影响**: 仅在未来移除/重命名 provider 时显现；当前 9 个 ID 全覆盖旧卡，暂无现实触发
- **建议改法和验证点**: compose 时删除 dest 中「形如 model_providers.* 但既不在 shared 也非 dest 自有标记外」的段，或为 app home 也引入 managed 块语义。验证点：注册表删项后重 compose,dest 不再含该项
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Open Questions

- Codex 的 session-resume bootstrap 究竟用 recorded `model_provider` 做什么：仅做存在性校验/配置解析，还是会在新卡生效前用它发请求？finding 3 的实际用户影响完全取决于此。仓库内无法证实（需读 Codex 源码或实测 resume 旧会话并抓 shim 日志）。
- Codex CLI / 桌面 app 写回 `~/.codex/config.toml`（trust 条目、`[desktop]` 等）时是否保留注释？finding 4 的触发条件。可通过「app 内改动一次状态 → diff config.toml」实测确认。
- 「卡片一律不得定义 provider」成为硬约束后，未来需要 profile 私有网关（非共享、非全局可解析）的场景是否只能改注册表+全局暴露？当前 12 个 profile 无此需求，但约束已固化进 composer 的 raise。

## Residual Risk

- Test gaps（`tests/test_provider_registry.py` 4 个用例全部通过，但仅覆盖 happy path + 单冲突用例）：未覆盖 base 缺失（finding 1）、旧卡 raise（finding 2）、标记注释丢失后的 conflict 路径（finding 4）、malformed TOML（base/registry 各自）、标记倒置/重复、并发修改中止路径（:72-73）、注册表删减后的 dest 残留（finding 6）、symlink base（finding 5）。
- 一致性无锁定：`model-providers.toml` 的端口↔`shim-routes.json` 的路由↔`agent-tools.zsh:_codex_agent_shim_port`（:285-297）三处端口事实目前人工对齐（本次已程序化验证注册表↔路由一致；local_llama 8080 直连无路由属设计），无任何测试防止未来漂移。
- CI gap：仓库无 CI 配置，`tests/` 只能手工运行，合入前是否执行依赖人。
- 未检查区域：Codex 二进制内部行为（resume 解析语义、config 写回格式）；`patch-codex-model-catalog.py` 与 shim 本体未改动未深读。
- 直连回退文档弱化：旧卡中「直连原值；停用 shim 时改回本行」的完整直连 base_url（含 `/api/v1`、`/coding/v1`、`/compatible-mode/v1` 路径）随卡片清理删除，README 表格只保留 host；停用 shim 时需手工重建完整 URL。属轻微运维回退。
- compose 输出的 app 配置会带一个孤儿 `# END code-is-cheap managed model providers` 注释（BEGIN 与注册表头注释被 `split_sections` 并入 shared 的前一节而未被复制；实测 dest 已存在时输出只有 END 无 BEGIN）。纯注释、无语义影响，但反映文本叠加对注释归属敏感。
- `compose-codex-app-config.py:94` 的 `write_text` 非原子（pre-existing）：同一 agent 的第二个 app 实例 launch 时会原地重写正在运行的实例的 config；非本次引入，仅记录。

## Validation Performed

- `python3 -m unittest discover -s tests -v`：4/4 通过。
- 真实 `sync-codex-agent.sh`（`CODEX_AGENT_TARGET`/`CODEX_SHARED_HOME` 指向 tmp）在 base 缺失时 exit 2 且零安装（finding 1 端到端实证）。
- 新 composer + 旧格式卡 → 未捕获 ValueError traceback、exit 1（finding 2 实证）；实机 9 张已安装卡当前全部为旧格式。
- `sync-codex-providers.py`：真实 base 语义（含 `[projects.*]`/`[desktop]`/notify）首次写入 + 二次运行幂等（`already current`）；冲突 base 干净中止且不改动文件。
- compose 到已存在 dest：共享注册表覆盖旧 provider 段、dest 状态保留、孤儿 END 注释实证、注册表外 provider 段残留实证。
- 注册表端口↔shim-routes.json 程序化交叉验证：8/8 shim 端口命中，local_llama 直连无路由（设计）。
- 实机 `~/.codex/config.toml`：当前无 `[model_providers.*]`、无标记 → 本机首次 sync 可走干净追加路径，不触发 conflict。
