RUNTIME/PROVIDER/MODEL: claude/kimi/kimi-k3[1m]
CANARY=kimi-6d4c9618

# Code Review

## Scope

- Mode: current changes（第三轮：controller 修复后的复审，基于 `code-review-20260925-083810.md` 与 `code-review-20260925-084205.md`）
- Branch or PR: `fix/shared-provider-resume`（无领先 main 的提交；`git merge-base main HEAD` = `4aa780f` = HEAD，全部改动仍为未提交的 working tree 改动 + untracked 路径）
- Base: `main`（`4aa780fb606154c5ff7acda3d94d5bbda1cb7c63`）
- Output file: `docs/reviews/code-review-20260925-090353.md`
- Included scope:
  - 新增：`codex-agent/model-providers.toml`（9 个共享 provider，含直连 fallback 注释）、`scripts/sync-codex-providers.py`、`tests/test_provider_registry.py`（7 个用例）
  - 修改：`scripts/sync-codex-agent.sh`（registry 同步先于卡片部署）、`scripts/compose-codex-app-config.py`（旧卡容错：告警 + 保留卡内路由）、9 张 profile 卡（移除 `[model_providers.*]`；glm-flash/mimo-fast/mimo-flash 改用独立 ID）、`README.md`/`README.zh.md`
  - 关联走读（契约面，未改动）：`scripts/agent-tools.zsh`（`_codex_agent_app` compose 调用点 :399-401、`_codex_agent_key_name`/`_codex_agent_shim_port` 映射 :272-297）、`codex-agent/shim-routes.json`、`scripts/sync-agent-tools.sh`（:14 安装 composer 到 `~/.local/bin`）、`scripts/patch-codex-model-catalog.py`
- Excluded scope: 凭证文件、session 会话内容（任务禁止）；`~/.codex/config.toml` 仅做计数 grep（未读正文）；Codex 二进制内部行为；`business` 独立 home
- Parallel review coverage: 无（任务禁止派发 sub-agent）

## 前轮 Findings 复核状态

### 083810-1（中，base 缺失时 sync 中止）→ **已修复（端到端实证）**
- 修复：`sync-codex-providers.py:59-61` 用 `existed = base.exists()` + `original = ""` 支持缺失 base；`:72` 新建文件 fchmod 0600
- 证据：真实 `sync-codex-agent.sh`（`CODEX_AGENT_TARGET`/`CODEX_SHARED_HOME` 指向 $TMPDIR 空目录；`codex`/`launchctl` 用 inert stub 隔离活机副作用）→ exit 0，base 自动创建（0600、含标记块）、12 张卡、bin×2、shim-routes.json 全部安装；测试 `test_sync_creates_missing_base_privately_and_refuses_malformed_registry` 锁定 0600 与字节级幂等
- 残余：无（catalog 生成失败仅 WARNING 不阻塞，为既有设计）

### 083810-2 / 084205-1（中，composer 对旧卡 raise 致桌面 app 启动崩溃）→ **已修复（实证）；次级路径残留**
- 修复：`compose-codex-app-config.py:89-95` 改为 stderr 告警（`warning: legacy provider table ... re-sync Codex cards`）+ 保留旧卡自带路由（卡内段覆盖共享注册表同 ID 段，迁移窗口内保持旧网关语义）
- 证据：用 `git show HEAD:` 取真实旧格式 mimo-fast 卡 + 新 composer → exit 0、告警命中、合成结果 9 个 provider 且 `mimo`→8794（旧卡路由保留，而非共享注册表的 8791）；测试 `test_legacy_card_keeps_its_gateway_during_upgrade` 锁定该行为
- **次级残留（084205-1 证据中的次要同源问题，未修复）**：`compose-codex-app-config.py:77` 无条件读 `--base`，base 缺失时即使 `--out` 已存在仍以 `FileNotFoundError` traceback 崩溃（实测 exit 1）。可达成性已降低（sync 现在会自建 base），仅剩「base 被删而卡仍在」场景；形态仍是 traceback 而非单行诊断
- 实机状态：live `~/.local/bin/compose-codex-app-config.py` 与仓库新版逐字节一致（cmp 通过），`~/.codex/config.toml` 已有标记块（9 provider、1 BEGIN），12 张已安装卡全部为新格式（0 张含 `model_providers`）——本机迁移窗口已关闭

### 083810-3（中，legacy 共享 ID 解析到普通变体网关/凭证）→ **未修复（已披露的设计取舍，维持原判）**
- 现状：CLI resume 路径不变，旧 flash/fast 会话的 recorded `glm`/`mimo` 仍解析到 8789/8791 普通变体；README.md:259-261 与 README.zh.md 对应段落的披露保留
- 结构性无法无损修复（旧会话 recorded ID 不含变体信息）；实际影响仍取决于 Codex resume bootstrap 是否发请求（Open Questions 沿用）
- 迁移窗口内桌面侧有改善：旧卡 compose 保留卡内路由（083810-2 的修复副作用）；但反向 transient 见 Residual Risk 第 3 条

### 083810-4（低，managed 块所有权押在可丢失的注释标记上）→ **未修复（结构性，维持原判）**
- 现状：`:17-18` 标记仍为 TOML 注释；实测 markerless base（含未标记 `model_providers.kimi`）→ exit 2 `already owned` 冲突中止、文件不动——fail-closed 行为不变
- 触发条件（Codex/app 全量重写丢注释）仍无法从仓库证实（Open Questions 沿用）

### 083810-5（低，TOCTOU + symlink）→ **symlink 部分已修复；TOCTOU 残余保留**
- symlink：`:55-56` 拒绝 symlink base（ap.error 干净退出）；实测 exit 2 且 symlink 本身保留；测试 `test_sync_rejects_symlink_and_damaged_block_without_writes` 锁定
- 并发检测增强：`:77` 重读比对现在同时检查 symlink 化、存在性翻转、内容变化。但检查（:77）与 `os.replace`（:79）之间窗口仍在（微秒级）；rename 后未 fsync 目录；SIGKILL 留 `.config.toml.*` 孤儿。维持残余风险，不升级为 finding

### 083810-6（低，注册表删项后 app home 残留）→ **未修复（实证仍在）**
- 现状：`compose-codex-app-config.py:86-88` 仍只有覆盖/新增、无删除。实测：dest 预置 `[model_providers.retired]`（不在注册表）→ compose 后完整存活（`desktop.state` 与共享 `kimi` 段均正确）
- 触发仍需未来移除/重命名 provider；当前 9 ID 全覆盖旧卡，无现实触发

### 084205-2（中，失败路径无测试、跨文件契约无锁定）→ **大部分已修复；契约锁定仍缺**
- 已补：测试 4→7。新增覆盖：全卡↔注册表解析（含变体 base_url 两两不等）、缺失 base 创建 + 0600 + 字节级幂等、malformed registry 拒绝且不写、symlink 拒绝 + 损坏块拒绝且不写、app home 刷新保留状态、旧卡迁移行为。README.md:544-548 新增 `python3 -m unittest discover -s tests` 入口
- 仍缺：(a) registry↔`shim-routes.json`↔`agent-tools.zsh` 三处端口/env_key 的一致性测试（本次已程序化验证全对，见 Validation，但无测试防漂移）；(b) 并发修改中止路径（`:77`）无测试；(c) 倒置标记诊断无测试；(d) 仓库仍无 CI，测试靠手工

### 084205-3（低，registry 接受非表值）→ **已修复（实证）**
- 修复：`:24-25` 增加 `any(not isinstance(value, dict) ...)` 校验
- 证据：实测 `[model_providers]\noops = 1` + `[model_providers.kimi]...` → exit 2 干净报错 `registry must contain only nonempty [model_providers.*] tables`，base 字节不变；测试覆盖

### 084205-4（低，直连 fallback URL 随卡片删除丢失）→ **已修复**
- 修复：`model-providers.toml` 8 个 shim 化 provider 各带 `# Direct fallback when disabling the shim: <完整含路径 URL>` 注释；`local_llama` 的直连值即其 active base_url（8080）；README 同步改写
- 旧 local 卡注释里的 8793 shim 路由已过时（8793 现为 mimo_flash 端口），删除正确

## 本轮独立检查（migration/error 路径回归扫描）

- **config ownership**：未标记 managed ID → 拒绝（exit 2，实证）；块外 `[projects]`/根键保留（测试）；块整段替换语义不变。修复未削弱所有权模型
- **atomic writes**：同目录 mkstemp + fchmod（已存在则 S_IMODE 保留权限——实测 0644 空 base 同步后仍 0644；新建 0600）+ fsync + 重读比对 + os.replace；全部错误路径（malformed/symlink/损坏块/倒置/冲突）实测零写入
- **old-card coexistence**：CLI 侧旧卡自带 provider 段在 `-p` 分层下压过 base（与 main 行为一致）；桌面侧 compose 保留卡路由（实证）；旧卡 + 新注册表共存无冲突（合成结果 9 provider 齐全、变体 ID 在场）
- **shared provider IDs**：12 张卡 0 张定义 provider；9 个注册表 ID 与卡 `model_provider` 一一对应；`glm_flash`/`mimo_fast`/`mimo_flash` 独立端口 8795/8794/8793。程序化交叉验证 registry↔shim-routes.json↔`_codex_agent_shim_port`/`_codex_agent_key_name`：9/9 OK（local 直连无路由属设计）
- **fresh-base sync**：真实脚本端到端 exit 0 全量安装（见 083810-1）
- **old-card desktop compatibility**：真实 git-HEAD 旧卡 compose 成功（见 083810-2）
- **malformed registry rejection**：exit 2 + 零写入（见 084205-3）
- **symlink rejection**：exit 2 + symlink 保留（见 083810-5）
- **test strength**：7/7 通过（0.23s）；覆盖面见 084205-2 状态；缺口已列明

### 新发现问题（本轮）

无新增正式 finding。修复引入的代码走读未发现回归；以下为维持原判的残余项，合并记录于 Residual Risk。

## Open Questions

- （沿用）Codex session-resume bootstrap 对 recorded `model_provider` 的实际使用（仅解析校验还是发请求）——决定 083810-3 的实际影响面，仓库内不可证实。
- （沿用）Codex CLI / 桌面 app 写回 `~/.codex/config.toml` 是否保留注释——决定 083810-4 的触发条件。实机现已带标记块，可通过「app 内改一次状态 → diff 标记是否存活」实测。
- `patch-codex-model-catalog.py:53` 硬编码 `SHARED_HOME = HOME / ".codex"`，不响应 `CODEX_SHARED_HOME`——`sync-codex-agent.sh` 的 env 覆盖对该步不完整（既有问题，本轮 probe 因此需 stub 隔离）。是否有意？

## Residual Risk

1. **compose 缺 base 仍 traceback**（084205-1 次级，未修复）：`compose-codex-app-config.py:77` 无条件读 base；base 被删而卡仍在时桌面启动以 `FileNotFoundError` traceback 失败（实测 exit 1）。同理全新机器首跑 app（卡也缺失）也是 traceback。均为「安装不完整」场景，建议未来改为单行诊断。
2. **倒置标记诊断质量**（沿用）：END 在 START 前时报 `not enough values to unpack (expected 2, got 1)`（实测 exit 2、零写入、fail-closed），难以定位；`:29` 的计数检查不覆盖顺序。
3. **迁移窗口反向 transient（新记录的细微语义）**：窗口内用旧变体卡 compose 的 app home 把共享 ID 映射到变体端口（如旧 mimo-fast 卡 → `mimo`=8794），此时在该 app 内 resume 一条普通 mimo 会话会打到变体网关；re-sync 后自愈。与 083810-3 同族、方向相反、瞬时。
4. **app home 残留与孤儿注释**（沿用，均实测仍在）：注册表外 provider 段在 app home 永久存活（083810-6）；合成输出带孤儿 `# END` 注释（BEGIN 归属前一节未被复制，实测输出第 79 行只有 END）；`write_text` 非原子（pre-existing）。
5. **原子性残余**（沿用）：`:77` 检查与 `:79` replace 间 TOCTOU 窗口；rename 后无目录 fsync；SIGKILL 留 `.config.toml.*` 孤儿临时文件；`--base` 父目录不存在时 mkstemp 抛未捕获 traceback（sync 脚本会 `mkdir -p`，仅直接误用触发）。
6. **契约漂移无测试锁定**（084205-2 残余）：registry 端口/env_key 与 `shim-routes.json`、`agent-tools.zsh` 三处真源靠人工对齐（本次验证一致）；无 CI，测试入口已写进 README 但执行靠人。
7. **文档小缺口**（沿用/既有）：README「Edit only the source files」清单仍缺 `compose-codex-app-config.py`；`tests/__pycache__` 已由 `.gitignore:3` 覆盖不会误入库。

## Validation Performed

- `python3 -m unittest discover -s tests -v`：7/7 通过。
- 端到端 fresh-base：真实 `sync-codex-agent.sh` + $TMPDIR 空 home（`codex`/`launchctl` stub 隔离活机）→ exit 0；base 自建（0600、标记块、注册表头注释在）、12 卡、bin×2、routes 安装；二次运行 `already current` 幂等。
- 旧卡桌面兼容：`git show HEAD:` 真实旧 mimo-fast 卡 + 新 composer → exit 0、stderr 告警、`mimo`→8794 保留、9 provider 齐全、`mimo_fast`/`glm_flash` 在场。
- 错误路径探针（全部 $TMPDIR）：malformed registry（非表值）exit 2 零写入；symlink base exit 2 链接保留；倒置标记 exit 2 零写入（报错文案难懂）；markerless managed ID exit 2 冲突拒绝；缺 base + 有 out 的 compose exit 1 traceback（残留）；app home `retired` 段残留 + 孤儿 END（残留）。
- 权限：新建 base 0600；已存在 0644 base 同步后保持 0644（S_IMODE）。
- 跨文件契约程序化核对：12 卡 ↔ registry ↔ shim-routes.json ↔ zsh 端口/key 映射 9/9 一致；12 卡 0 provider 段；registry 9 ID 无重复（tomllib 解析）。
- 实机状态（计数 grep，未读正文）：live base 已含标记块（9 provider、1 BEGIN）；12 张已安装卡全部新格式；`~/.local/bin/compose-codex-app-config.py` 与仓库新版逐字节一致——本机已完成迁移。
- 语法：`bash -n`（前轮已验，本轮 diff 未再动 sync 脚本主体）、两 Python 脚本实际运行通过。
