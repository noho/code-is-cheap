RUNTIME/PROVIDER/MODEL: claude/mimo/mimo-v2.6-pro[1m]
CANARY=mimo-7c9e93a4

# Code Review

## Scope

- Mode: current changes
- Branch or PR: `fix/shared-provider-resume`（`git diff main...HEAD` 为空，全部改动为 unstaged/untracked 工作区改动；本次为 controller 修复后的复审）
- Base: `main`（`4aa780fb606154c5ff7acda3d94d5bbda1cb7c63`）
- Output file: `docs/reviews/code-review-20260925-090337.md`
- Included scope:
  - `codex-agent/model-providers.toml`（新增，9 个共享 provider + 直连 fallback 注释）
  - `scripts/sync-codex-providers.py`（新增，带标记块原子同步器；本轮修复后的版本）
  - `scripts/sync-codex-agent.sh`（registry 同步先于卡片部署）
  - `scripts/compose-codex-app-config.py`（注入共享 registry；旧卡降级为 warn + 保留旧路由，不再 raise）
  - `codex-agent/profiles/{ds-flash,glm,glm-flash,kimi,local,mimo,mimo-fast,mimo-flash,qwen}/config.toml`（移除卡内 `[model_providers.*]`；glm-flash/mimo-fast/mimo-flash 改用独立 provider ID）
  - `tests/test_provider_registry.py`（7 个用例）
  - `README.md` / `README.zh.md`
  - 契约面走读（未改动）：`scripts/agent-tools.zsh`（`_codex_agent_app` 合成链、`_codex_agent_launch` 的 `-p`/resume 接线、`_codex_agent_key_name`/`_codex_agent_shim_port` 映射）、`codex-agent/shim-routes.json`
- Excluded scope: 凭据与会话内容（按约束不读取；旧卡内容取自 `git show main:...` 而非 live home）；`~/.codex-agent/business`（独立 home）；Codex 二进制内部 resume/config 写回语义（闭源，见 Open Questions）；`skills/`、`codex-agent/bin/`（本 diff 未触及）
- Parallel review coverage: 无（按任务约束不派发 sub-agent；全部走读由主 reviewer 完成）
- 复审任务指定检查项执行情况：fresh-base sync ✅、old-card desktop 兼容 ✅、malformed registry 拒绝 ✅、symlink 拒绝 ✅、test strength ✅（详见 Validation Performed 与 findings）

## Findings

### 1-未修复-低-registry 空表通过校验并写入 live base，与"nonempty tables"契约不符
- **入口/函数**: `scripts/sync-codex-providers.py` `compose()` 的 registry 形状校验
- **文件(行号)**: `scripts/sync-codex-providers.py:24-26`（`any(not isinstance(value, dict) ...)` 只查类型不查空）
- **输入场景**: `codex-agent/model-providers.toml` 被手工编辑后留下空表，例如 `[model_providers.kimi]` 下无任何键
- **实际分支**: 空表解析为 `{}`，`isinstance({}, dict)` 为真 → 校验通过 → `compose()` 正常返回并写入 live base
- **预期行为**: 按同函数报错文案 "registry must contain only nonempty [model_providers.*] tables"，空表应被拒绝
- **实际行为**: 实测 registry 为 `[model_providers.kimi]\n\n[model_providers.ok]\n...` 时 rc=0，base 被改写且 `model_providers` 含 `'kimi': {}`。Codex 将看到无 `base_url`/`env_key` 的 provider 定义，任何记录该 ID 的会话（含 resume）解析出的网关不可用，故障延迟到请求时才暴露
- **直接证据**: `sync-codex-providers.py:24-25` 的 `any()` 仅 `not isinstance(value, dict)`；探针 `P5`：`rc=0 written providers: {'kimi': {}, 'ok': {...}}`。对照：084205 finding 3 的非表值（`oops = 1`）现已被正确拒绝（`P1`），说明修复只覆盖了标量而未覆盖空表
- **影响**: 人工改坏 repo 文件后 sync 静默把缺字段的 provider 写入机器 live 配置；影响全部 profile 的 resume 解析面
- **建议改法和验证点**: 把校验改为 `any(not isinstance(value, dict) or not value for value in providers.values())`；补一条空表负例测试（现有 `test_sync_creates_missing_base_privately_and_refuses_malformed_registry` 可直接扩展）。验证点：空表 registry → rc!=0 且 base 原文不变
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 2-未修复-低-sync 写路径错误面有缺口：非法 base 根键类型与 base 消失竞态均以 traceback 退出
- **入口/函数**: `scripts/sync-codex-providers.py` `compose()` / `main()`
- **文件(行号)**: `sync-codex-providers.py:38-39`（`tomllib.loads(outside).get("model_providers", {})` 后直接 `set(existing)`）；`sync-codex-providers.py:72`（`base.stat()` 在受控 `except` 之外）
- **输入场景**: (a) base 根键被手工写成非表类型（`model_providers = 5`）；(b) sync 读入 base 后、写权限前 base 文件被并发删除/移走
- **实际分支**: (a) `set(5)` 抛 `TypeError`，而 `main()` 只捕获 `(OSError, ValueError, tomllib.TOMLDecodeError)`（:63）→ 未捕获 traceback；(b) `existed=True` 时 `base.stat()` 抛 `FileNotFoundError`（属 OSError 但不在 :60-64 的 try 覆盖段内，在 :70 之后）→ 未捕获 traceback
- **预期行为**: 与其它坏输入一致：单行 `ap.error` 诊断、非零退出、不写文件
- **实际行为**: (a) 实测 `model_providers = 5` → rc=1 `TypeError: 'int' object is not iterable`，base 未改（fail-closed 但无诊断价值）；同输入的 string/array 变体虽 rc=2 但报的是合成产物的 TOML 错（"Cannot overwrite a value"），指错位置；(b) 为静态可见缺口（:72 在 try/except 之外），未实测竞态
- **直接证据**: 探针 `P5`：`model_providers-scalar: rc=1 err=TypeError: 'int' object is not iterable`；`sync-codex-providers.py:63` 捕获类型列表不含 TypeError；:70-82 的 try 只有 finally 无 except
- **影响**: 仅人工改坏 base 或极端并发下的诊断质量（不会写坏文件）；报错形态为 Python traceback，与该脚本其它路径的干净退出不一致
- **建议改法和验证点**: 对 `existing` 做类型断言（非 dict 即报 "base model_providers must be a table"）或把 TypeError 纳入捕获；把 :70-82 写入段整体纳入 `except OSError` → `ap.error`。验证点：`model_providers = 5` 的 base → rc=2 且 stderr 为单行错误
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 3-未修复-低-缺 base 且 out 已存在时 compose 以 FileNotFoundError traceback 崩溃（084205#1 次要问题未修）
- **入口/函数**: `scripts/compose-codex-app-config.py` `main()`；调用点 `scripts/agent-tools.zsh:399-400`（app 每次启动都调用）
- **文件(行号)**: `scripts/compose-codex-app-config.py:77`（`shared_text = Path(args.base).read_text()` 无条件读取）
- **输入场景**: 共享 base `~/.codex/config.toml` 在 sync 之后被删除/移走，而 app home 的 `config.toml` 仍存在（例如手工清理或 dotfiles 迁移失误）；随后启动任一桌面 profile
- **实际分支**: :77 无条件读 base → `FileNotFoundError` 未捕获 → traceback rc=1；`agent-tools.zsh:400` 的 `|| return 1` 使 app 不启动
- **预期行为**: out 存在时可降级为"无共享 registry 可注入"（沿用 out 现状 + 告警），或至少给出单行可行动错误；main 版本此场景可正常工作（out 存在时不读 base，见 `git diff` 前的 `base_text = out_path.read_text() if out_path.exists() else ...`）
- **实际行为**: 实测 rc=1，stderr 末行 `FileNotFoundError: [Errno 2] No such file or directory: '.../gone.toml'`（探针 `P4d`），完整 traceback
- **直接证据**: `compose-codex-app-config.py:77` 无条件 `read_text()`；本分支 diff 将 main 的惰性读取改为立即读取（新增共享 registry 注入需要 base）；`_codex_agent_require_home`（agent-tools.zsh:334-337）只检查卡片存在，不检查 base，故该场景能走到 compose
- **影响**: 用户可见的桌面 app 启动失败（traceback 形态）；触发面窄（需 base 单独缺失）且 fail-closed，但相对 main 为回归
- **建议改法和验证点**: base 缺失且 out 存在时以 stderr 告警跳过 shared 注入（或 `sys.exit` 单行错误提示重跑 sync-codex-agent.sh）；base 与 out 均缺失时同理单行报错。验证点：P4d 场景 rc=0（带告警）或 rc=2 单行错误，无 traceback
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

### 4-未修复-低-跨文件 provider 契约仍无回归锁定，失败路径测试留有缺口（084205#2 修复余量）
- **入口/函数**: `tests/test_provider_registry.py`；被测契约面 `model-providers.toml` ↔ `shim-routes.json` ↔ `agent-tools.zsh` 的 `_codex_agent_key_name`/`_codex_agent_shim_port`
- **文件(行号)**: `tests/test_provider_registry.py:25-34`（仅 registry 内部 3 对 base_url 互不相等 + 卡片 ID 可解析）；空白面：`sync-codex-providers.py:77`（并发修改守卫无测试）、:31-33（倒置 marker 的诊断无测试）、跨文件端口/env_key 一致性无任何断言
- **输入场景**: 未来改动 registry 端口/env_key、`shim-routes.json` 路由或 zsh case 表任意一处时
- **实际分支**: 现有 7 例已覆盖 missing-base 创建、malformed registry 拒绝、symlink 拒绝、单侧 marker 损坏拒绝、旧卡降级 + 升级收敛（084205#2 的 (a 单侧)(c) 已补），但 (b 并发守卫)、(d 跨文件契约) 未覆盖
- **预期行为**: registry 的 `base_url` 端口 ↔ `shim-routes.json` 的 `port`/`upstream` ↔ `_codex_agent_shim_port`/`_codex_agent_key_name` 由测试锁定；`main()` 的"changed during sync"分支至少有一条可注入的回归测试
- **实际行为**: 本次人工程序化核对 9/9 一致（探针 `P6`：port、env_key、fallback host 全部 OK），但契约跨 3 个文件、含重复真源，任何一侧漂移都不会被测试发现；倒置 marker 仍报出 `not enough values to unpack (expected 2, got 1)`（探针 `P3`，:30 计数检查放行 1/1 后 :33 `split(END)` 失败），fail-closed 但不可定位；:77 守卫完全未被测试执行
- **直接证据**: `python3 -m unittest discover -s tests -v` → 7 tests OK，无任何用例读取 `shim-routes.json` 或 `agent-tools.zsh`；`P6` 显示一致性成立但无锁定；`P3` 显示倒置 marker 诊断仍是 Python 内部报错
- **影响**: 回归防护缺口——端口/env_key 漂移（错网关/错凭证，与 legacy ID 残余同型）或并发守卫退化时测试全绿；倒置 marker 的运维诊断不可用
- **建议改法和验证点**: 新增一致性测试（解析 registry 的 `:(\d+)/` 端口与 `shim-routes.json` 交叉比对；zsh case 表可用正则提取或抽成数据文件）；为 :77 注入"读入后改写"的可测钩子或至少加倒置 marker 负例断言错误文案；README 已有测试入口（README.md:544-548），建议再补 CI
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 低

## Prior Findings Verification（对 20260925-083810 / 20260925-084205 的复核）

| 出处 | 摘要 | 状态 | 证据 |
|---|---|---|---|
| 083810#1（中） | base 缺失时整个 sync 中止 | **已修复** | `sync-codex-providers.py:59-61` 改为 `existed=base.exists()`、缺省 `original=""` 走全新安装；探针 `P5`：fake HOME 真跑 `sync-codex-agent.sh` exit 0，12 卡 + 9 provider + bin + routes + catalogs 全部安装，base mode 600，二次运行幂等；`test_sync_creates_missing_base_privately_and_refuses_malformed_registry` 锁定 |
| 083810#2 / 084205#1（中） | 旧格式卡令 compose 崩溃、升级窗口桌面 app 不可用 | **已修复**（次要问题见 finding 3） | raise 已移除，改为 warn + 保留卡内旧路由（`compose-codex-app-config.py:89-95`）；探针 `P4a/P4b/P4c`：真实 main 版旧卡（kimi、mimo-fast）rc=0 且旧路由生效（mimo@8794 覆盖共享 8791），re-sync 新卡后同 out 收敛回共享 registry（mimo@8791 + mimo_fast@8794，`model_provider=mimo_fast`）；`test_legacy_card_keeps_its_gateway_during_upgrade` 锁定告警与保留语义 |
| 083810#3（中） | legacy 共享 ID（glm/mimo）resume 解析到普通变体路由 | **按设计保留并文档化** | README.md:256-261 / README.zh.md 均明示 "Legacy sessions recorded with the shared `glm` or `mimo` IDs cannot identify which variant created them; those IDs retain the normal `glm` and `mimo` routes"；行为与 084205 复审一致，无代码变化（旧会话 ID 本身不含变体信息，属不可无损修复） |
| 083810#4（低） | 所有权标记押在可丢失的 TOML 注释上 | **未修复（按设计保留）** | 探针 `P3` `marker-lost-providers-remain`：标记丢失后仍以 "provider IDs already owned outside managed block" 中止（fail-closed、base 不改）；Codex/app 写回是否丢注释仍未知（Open Questions） |
| 083810#5（低） | symlink base 被 os.replace 静默替换；TOCTOU 残余 | **已修复（symlink 部分）**；TOCTOU 残余见 Residual | `sync-codex-providers.py:55-56` 入口拒绝 + :77 替换前复查 `is_symlink()`；探针 `P2`：rc=2、单行错误、symlink 与 target 均未动；`test_sync_rejects_symlink_and_damaged_block_without_writes` 锁定 |
| 083810#6（低） | 注册表删项后 app home 残留旧 provider | **未修复** | 探针 `P4f`：预置 `[model_providers.retired]` 的 out 经 compose 后 `retired` 仍存活（`compose-codex-app-config.py:86-95` 只赋值不删除）；当前 9 个 ID 无现实触发面，保留为 residual |
| 084205#2（中） | tests 只盖 happy path、跨文件契约无锁定 | **部分修复** | 4→7 例，新增 missing-base 创建/权限/幂等、malformed registry 拒绝、symlink + 单侧 marker 损坏拒绝、旧卡降级、app home 刷新；README 增加 `python3 -m unittest discover -s tests` 入口（README.md:544-548）。余量升格为 finding 4：并发守卫、倒置 marker 诊断、跨文件端口/env_key 一致性仍无测试；仓库仍无 CI |
| 084205#3（低） | registry 接受 `[model_providers]` 下非表值 | **已修复**（空表余量见 finding 1） | `sync-codex-providers.py:24-25` 增加 `any(not isinstance(value, dict) ...)`；探针 `P1`：`oops=1`、`kimi=1`、空 registry、顶层杂散键全部 rc=2 且 base 不变；`test_sync_creates_missing_base_privately_and_refuses_malformed_registry` 锁定 |
| 084205#4（低） | 删除卡片丢失直连 fallback URL | **已修复** | `model-providers.toml` 每表恢复 `# Direct fallback when disabling the shim: <含路径完整 URL>`；与 main 版旧卡逐一比对 8/8 一致（deepseek `/v1`、glm `/api/v1`、glm_flash `/api/v1`、kimi `/coding/v1`、mimo `/v1`、mimo_fast `/v1`、mimo_flash `/v1`、qwen `/compatible-mode/v1`）；local_llama 本就直连无需 fallback。残余：local 旧卡"已停用 shim 路由（8793，guardian 超时）"注释未迁移，见 Residual |

## Open Questions

- resume 修复的前提（会话记录的 provider 需在 base config 中可解析，`codex resume`/`-p` 才能跨卡续会话）无法从仓库验证：依赖 Codex 闭源的 config 分层与 thread provider 解析行为。本 review 按 README/分支声明接受该前提。若前提不成立，整个改动的收益面需重新评估。
- Codex CLI / 桌面 app 写回 `~/.codex/config.toml` 时是否保留注释（083810#4 的触发条件）仍未实测。可通过"app 内改一次状态 → diff config.toml"确认；若丢注释，需结构性标记。
- Codex 对空 provider 表（finding 1 的 `'kimi': {}`）的解析行为未知——是启动报错、请求失败还是静默忽略。决定 finding 1 的实际影响等级。

## Residual Risk

- **旧变体线程静默换模型（083810#3 延长线）**：legacy `glm`/`mimo` 会话 resume 落到普通路由后，shim 会按 `shim-routes.json` 的 `to` 改写模型名（如 → `glm-5.3`、→ `mimo-v2.6-pro`），对话以错误变体继续且无报错。README 已披露取舍，但失败模式仍是静默的。
- **TOCTOU 残余**：`sync-codex-providers.py:77` 的重读比较与 `os.replace` 之间仍有窗口（守卫本身也无测试，见 finding 4）；`base.stat()`（:72）在受控 except 之外的消失竞态并入 finding 2；rename 后未 fsync 目录；SIGKILL 可留 `.config.toml.*` 孤儿临时文件。对本用途可接受。
- **app home 残留**：(a) 注册表删项后旧 provider 段在已合成 out 中永久存活（083810#6，`P4f` 复证）；(b) 预置 dest 合成时 `# END` marker 泄漏、`# BEGIN` 丢失（`P6` 复证，纯注释）。
- **测试执行靠人工**：README 已有入口（`python3 -m unittest discover -s tests`，本次 7/7 通过），但仓库无 CI，合入前是否执行依赖人。
- **README 清单缺口（既有问题延续）**："Edit only the source files"（README.md:522-536）与 `scripts/` 树仍缺 `scripts/compose-codex-app-config.py`（README:170 正文引用了它）与 `tests/`；本 diff 给清单新增了两行却未补齐被自己修改的 composer。
- **旧卡降级语义的告警措辞**：composer 对任何卡内 `[model_providers.*]` 都打 "legacy provider table ... re-sync"。若未来新格式卡回归地携带 provider 段（仓库测试可拦），运行时会以误导性"legacy"告警静默接受——仓库侧不变量与运行时执法之间留有一个月牙缝。
- **未检查面**：Codex 二进制对重复 provider ID 跨层（base vs card）的合并语义、`business` home、shim 服务实际转发行为、真实 resume 端到端（涉及会话数据，按约束不触碰）、`patch-codex-model-catalog.py` 与 shim 本体（未改动未深读）。

## Validation Performed

- diff 走读：`git diff`（unstaged 全量 13 文件）+ untracked 全文（`model-providers.toml`、`sync-codex-providers.py`、`tests/`）；`git diff main...HEAD` 为空（改动全在工作区）。旧卡对照取自 `git show main:codex-agent/profiles/*/config.toml`。
- 测试：`python3 -m unittest discover -s tests -v` → 7/7 OK；`bash -n scripts/sync-codex-agent.sh` + `py_compile` 两脚本通过。
- **fresh-base sync**（探针 `P5`，fake HOME 真跑 `sync-codex-agent.sh`）：exit 0；`~/.codex/config.toml` 新建（mode 600）含 managed block 与 9 provider；12 张卡、`bin/`（shim + service）、`shim-routes.json`、5 个 model catalog 全部安装；二次运行 exit 0 幂等。083810#1 场景闭环。
- **old-card desktop 兼容**（探针 `P4a-P4c`）：真实 main 版旧卡 + 新 composer → rc=0、stderr 含 "legacy provider table"、卡内旧路由生效（kimi@8790；mimo-fast 的共享 ID `mimo` 被卡内 8794 + `MIMO_API_KEY` 覆盖共享 8791）；9 个共享 provider 同时注入；换新卡重合成后收敛（`mimo`→8791、`mimo_fast`→8794、`model_provider=mimo_fast`）。
- **malformed registry 拒绝**（探针 `P1`）：`oops = 1`、`kimi = 1`、空 `[model_providers]`、顶层杂散键 → 全部 rc=2 且 base 原文不变；**空表 `[model_providers.kimi]` → rc=0 且写入 base**（finding 1）。
- **symlink 拒绝**（探针 `P2`）：rc=2 单行错误，symlink 保持、target 原文不变。083810#5 闭环。
- **marker 损坏**（探针 `P3`）：begin-only / end-only / duplicate → "incomplete or duplicate managed provider block"（干净）；inverted → "not enough values to unpack"（fail-closed 但诊断差，finding 4）；标记丢失残留 provider → "already owned outside managed block"（083810#4 语义不变）。
- **其它错误路径**（探针 `P5b`）：base 根键 `model_providers = 5` → rc=1 `TypeError` traceback（finding 2）；string/array 根键 → rc=2 但报合成 TOML 错（指错位置）。
- **缺 base compose**（探针 `P4d`）：rc=1 `FileNotFoundError` traceback（finding 3）。
- **跨文件契约**（探针 `P6`）：registry ↔ `shim-routes.json` ↔ `_codex_agent_key_name`/`_codex_agent_shim_port` 程序化交叉比对 9/9 一致（端口、env_key、fallback host 全 OK；local_llama 直连无路由属设计）；卡片 `model_provider` ↔ registry ID ↔ agent key/port 一一对应。
- TOML 有效性：registry + 12 张新卡 `tomllib` 解析通过；卡内无 `[model_providers.*]`；`tests/__pycache__` 已被 `.gitignore` 覆盖。
- 直连 fallback 注释与 main 版旧卡注释逐一比对 8/8 一致（084205#4 闭环）。
